name: ci

'on':
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Fail if plugin.json or marketplace.json were manually changed in a PR
  # These files are auto-generated by CD workflow on main
  check-version-files:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Fetch base branch
        run: git fetch origin ${{ github.base_ref }}:refs/remotes/origin/${{ github.base_ref }}

      - name: Check for manual version file changes
        shell: bash
        run: |
          # Check if any existing plugin.json or marketplace.json files were modified
          # Use --diff-filter=M to only catch modifications, not additions (new plugins need plugin.json)
          CHANGED_VERSION_FILES=$(git diff --diff-filter=M --name-only "origin/${{ github.base_ref }}..HEAD" -- \
            'plugins/*/.claude-plugin/plugin.json' \
            '.claude-plugin/marketplace.json' \
          )

          if [ -n "$CHANGED_VERSION_FILES" ]; then
            echo "❌ ERROR: Version files should not be manually changed in PRs!"
            echo ""
            echo "The following version files were modified:"
            echo "$CHANGED_VERSION_FILES"
            echo ""
            echo "Version bumps happen automatically when PRs are merged to main."
            echo "Please revert these changes:"
            echo "  git checkout origin/${{ github.base_ref }} -- $CHANGED_VERSION_FILES"
            exit 1
          fi

          echo "✅ No manual version file changes detected"

  lint:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Authenticate as GitHub App
        id: auth
        uses: ./.github/actions/github-app-auth
        with:
          app-id: ${{ secrets.AUTOMATION_GITHUB_APP_ID }}
          private-key: ${{ secrets.AUTOMATION_GITHUB_APP_PRIVATE_KEY }}

      - name: mise install -y
        uses: jdx/mise-action@v3
        with:
          install: true
          cache: true

      - name: just lint
        id: lint
        #
        uses: ./.github/actions/lint-files
        with:
          fix: 'true'
        continue-on-error: true

      - name: git push
        if: steps.lint.outputs.changed-or-failed == 'true' && ! cancelled()
        id: auto-commit-action
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: 'chore: `just lint`'

      - name: 'exit 1 # changes detected or linting failed'
        if: ${{ steps.lint.outputs.changed-or-failed == 'true' && ! cancelled() }}
        run: |
          echo "Changes made or lint failed! Auto-committed the fixes, but more lint and format issues may remain."
          exit 1

  validate:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: mise install -y
        uses: jdx/mise-action@v3
        with:
          install: true
          cache: true

      - name: Validate plugin structure
        uses: ./.github/actions/validate-plugins
