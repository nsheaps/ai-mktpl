name: Claude Agent

# This workflow runs Claude Code in agent mode when triggered by repository_dispatch
# Agent mode is automatically enabled for repository_dispatch triggers
#
# Event types:
# - claude-agent-mention: @claude mentions (uses claude-agent-system.md)
# - claude-agent-new-issue: New issues for planning (uses issue-planner-system.md)
#
# The system prompt template is specified in the payload, no detection logic needed here

on:
  repository_dispatch:
    types: [claude-agent-mention, claude-agent-new-issue]

jobs:
  claude:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false
          fetch-depth: 50

      - name: Install mise
        uses: jdx/mise-action@v3
        with:
          install: true

      - name: Authenticate as GitHub App
        id: auth
        uses: ./.github/actions/github-app-auth
        with:
          app-id: ${{ secrets.AUTOMATION_GITHUB_APP_ID }}
          private-key: ${{ secrets.AUTOMATION_GITHUB_APP_PRIVATE_KEY }}

      - name: Extract payload values
        id: payload
        env:
          PAYLOAD: ${{ toJson(github.event.client_payload) }}
        run: |
          # Extract values from payload
          REPO=$(echo "$PAYLOAD" | jq -r '.source.repo // ""')
          PR_NUMBER=$(echo "$PAYLOAD" | jq -r '.source.pr_number // ""')
          ISSUE_NUMBER=$(echo "$PAYLOAD" | jq -r '.source.issue_number // ""')
          TITLE=$(echo "$PAYLOAD" | jq -r '.content.title // ""')

          # Get system prompt template from payload (defaults to agent system prompt)
          TEMPLATE=$(echo "$PAYLOAD" | jq -r '.system_prompt_template // ".ai/prompts/claude-agent-system.md"')
          echo "TEMPLATE=$TEMPLATE" >> "$GITHUB_OUTPUT"

          echo "REPO=$REPO" >> "$GITHUB_OUTPUT"
          echo "PR_NUMBER=$PR_NUMBER" >> "$GITHUB_OUTPUT"
          echo "ISSUE_NUMBER=$ISSUE_NUMBER" >> "$GITHUB_OUTPUT"
          echo "COMMENT_ID=$(echo "$PAYLOAD" | jq -r '.source.comment_id // ""')" >> "$GITHUB_OUTPUT"
          echo "AUTHOR=$(echo "$PAYLOAD" | jq -r '.author.login // ""')" >> "$GITHUB_OUTPUT"
          echo "ASSOCIATION=$(echo "$PAYLOAD" | jq -r '.author.association // ""')" >> "$GITHUB_OUTPUT"
          echo "TRIGGER_TYPE=$(echo "$PAYLOAD" | jq -r '.trigger.type // ""')" >> "$GITHUB_OUTPUT"
          echo "TRIGGER_ACTION=$(echo "$PAYLOAD" | jq -r '.trigger.action // ""')" >> "$GITHUB_OUTPUT"
          echo "TITLE=$TITLE" >> "$GITHUB_OUTPUT"

          # Planning context
          echo "PLANNING_TRIGGER_REASON=$(echo "$PAYLOAD" | jq -r '.planning.trigger_reason // ""')" >> "$GITHUB_OUTPUT"
          {
            echo "ORIGINAL_ISSUE_BODY<<EOF_BODY"
            echo "$PAYLOAD" | jq -r '.planning.original_issue_body // ""'
            echo "EOF_BODY"
          } >> "$GITHUB_OUTPUT"

          # Build comment history section
          COMMENT_HISTORY=$(echo "$PAYLOAD" | jq -r '.planning.comment_history // ""')
          if [ -n "$COMMENT_HISTORY" ] && [ "$COMMENT_HISTORY" != "null" ] && [ "$COMMENT_HISTORY" != "" ]; then
            {
              echo "COMMENT_HISTORY_SECTION<<EOF_COMMENTS"
              echo ""
              echo "## Comment History"
              echo ""
              echo "$COMMENT_HISTORY"
              echo "EOF_COMMENTS"
            } >> "$GITHUB_OUTPUT"
          else
            echo "COMMENT_HISTORY_SECTION=" >> "$GITHUB_OUTPUT"
          fi

          # Build conditional context sections for template
          if [ "$PR_NUMBER" != "null" ] && [ -n "$PR_NUMBER" ]; then
            echo "PR_CONTEXT=This is related to PR #$PR_NUMBER" >> "$GITHUB_OUTPUT"
          else
            echo "PR_CONTEXT=" >> "$GITHUB_OUTPUT"
          fi

          if [ "$ISSUE_NUMBER" != "null" ] && [ -n "$ISSUE_NUMBER" ]; then
            echo "ISSUE_CONTEXT=This is related to issue #$ISSUE_NUMBER" >> "$GITHUB_OUTPUT"
          else
            echo "ISSUE_CONTEXT=" >> "$GITHUB_OUTPUT"
          fi

          if [ "$TITLE" != "null" ] && [ -n "$TITLE" ]; then
            {
              echo "TITLE_SECTION<<EOF_TITLE"
              echo "## Title"
              echo ""
              echo "$TITLE"
              echo "EOF_TITLE"
            } >> "$GITHUB_OUTPUT"
          else
            echo "TITLE_SECTION=" >> "$GITHUB_OUTPUT"
          fi

          # Extract the prompt
          {
            echo "PROMPT<<EOF_PROMPT"
            echo "$PAYLOAD" | jq -r '.prompt // ""'
            echo "EOF_PROMPT"
          } >> "$GITHUB_OUTPUT"

      - name: Interpolate system prompt
        id: prompt
        uses: ./.github/actions/interpolate-prompt
        env:
          REPO: ${{ steps.payload.outputs.REPO }}
          PR_NUMBER: ${{ steps.payload.outputs.PR_NUMBER }}
          ISSUE_NUMBER: ${{ steps.payload.outputs.ISSUE_NUMBER }}
          AUTHOR: ${{ steps.payload.outputs.AUTHOR }}
          ASSOCIATION: ${{ steps.payload.outputs.ASSOCIATION }}
          TRIGGER_TYPE: ${{ steps.payload.outputs.TRIGGER_TYPE }}
          TRIGGER_ACTION: ${{ steps.payload.outputs.TRIGGER_ACTION }}
          TITLE: ${{ steps.payload.outputs.TITLE }}
          PROMPT: ${{ steps.payload.outputs.PROMPT }}
          PR_CONTEXT: ${{ steps.payload.outputs.PR_CONTEXT }}
          ISSUE_CONTEXT: ${{ steps.payload.outputs.ISSUE_CONTEXT }}
          TITLE_SECTION: ${{ steps.payload.outputs.TITLE_SECTION }}
          # Planning-specific variables
          PLANNING_TRIGGER_REASON: ${{ steps.payload.outputs.PLANNING_TRIGGER_REASON }}
          ORIGINAL_ISSUE_BODY: ${{ steps.payload.outputs.ORIGINAL_ISSUE_BODY }}
          COMMENT_HISTORY_SECTION: ${{ steps.payload.outputs.COMMENT_HISTORY_SECTION }}
        with:
          template-file: ${{ steps.payload.outputs.TEMPLATE }}

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        env:
          # Settings ported from original claude.yaml workflow
          INPUT_SETTINGS_JSON: |
            {
              "permissions": {
                "additionalDirectories": ["/tmp/"],
                "allow": [
                  "Task",
                  "Glob",
                  "Grep",
                  "LS",
                  "ExitPlanMode",
                  "Read",
                  "Edit",
                  "Write",
                  "Replace",
                  "MultiEdit",
                  "NotebookRead",
                  "NotebookEdit",
                  "TodoWrite",
                  "WebSearch",
                  "WebFetch",
                  "Bash",
                  "BashOutput",
                  "KillBash",
                  "ListMcpResourcesTool",
                  "ReadMcpResourceTool",
                  "SlashCommand",
                  "Skill",
                  "mcp__github_comment__update_claude_comment",
                  "mcp__github_ci__get_ci_status",
                  "mcp__github_ci__get_workflow_run_details",
                  "mcp__github_ci__download_job_log",
                  "mcp__github__add_comment_to_pending_review",
                  "mcp__github__add_issue_comment",
                  "mcp__github__add_project_item",
                  "mcp__github__add_sub_issue",
                  "mcp__github__assign_copilot_to_issue",
                  "mcp__github__AssignCodingAgent",
                  "mcp__github__cancel_workflow_run",
                  "mcp__github__create_and_submit_pull_request_review",
                  "mcp__github__create_branch",
                  "mcp__github__create_gist",
                  "mcp__github__create_issue",
                  "mcp__github__create_or_update_file",
                  "mcp__github__create_pending_pull_request_review",
                  "mcp__github__create_pull_request",
                  "mcp__github__create_repository",
                  "mcp__github__delete_file",
                  "mcp__github__delete_pending_pull_request_review",
                  "mcp__github__delete_project_item",
                  "mcp__github__delete_workflow_run_logs",
                  "mcp__github__dismiss_notification",
                  "mcp__github__download_workflow_run_artifact",
                  "mcp__github__fork_repository",
                  "mcp__github__get_code_scanning_alert",
                  "mcp__github__get_commit",
                  "mcp__github__get_dependabot_alert",
                  "mcp__github__get_discussion",
                  "mcp__github__get_discussion_comments",
                  "mcp__github__get_file_contents",
                  "mcp__github__get_global_security_advisory",
                  "mcp__github__get_issue",
                  "mcp__github__get_issue_comments",
                  "mcp__github__get_job_logs",
                  "mcp__github__get_latest_release",
                  "mcp__github__get_me",
                  "mcp__github__get_notification_details",
                  "mcp__github__get_project",
                  "mcp__github__get_project_field",
                  "mcp__github__get_project_item",
                  "mcp__github__get_pull_request",
                  "mcp__github__get_pull_request_diff",
                  "mcp__github__get_pull_request_files",
                  "mcp__github__get_pull_request_review_comments",
                  "mcp__github__get_pull_request_reviews",
                  "mcp__github__get_pull_request_status",
                  "mcp__github__get_release_by_tag",
                  "mcp__github__get_secret_scanning_alert",
                  "mcp__github__get_tag",
                  "mcp__github__get_team_members",
                  "mcp__github__get_teams",
                  "mcp__github__get_workflow_run",
                  "mcp__github__get_workflow_run_logs",
                  "mcp__github__get_workflow_run_usage",
                  "mcp__github__IssueToFixWorkflow",
                  "mcp__github__list_branches",
                  "mcp__github__list_code_scanning_alerts",
                  "mcp__github__list_commits",
                  "mcp__github__list_dependabot_alerts",
                  "mcp__github__list_discussion_categories",
                  "mcp__github__list_discussions",
                  "mcp__github__list_gists",
                  "mcp__github__list_global_security_advisories",
                  "mcp__github__list_issue_types",
                  "mcp__github__list_issues",
                  "mcp__github__list_notifications",
                  "mcp__github__list_org_repository_security_advisories",
                  "mcp__github__list_project_fields",
                  "mcp__github__list_project_items",
                  "mcp__github__list_projects",
                  "mcp__github__list_pull_requests",
                  "mcp__github__list_releases",
                  "mcp__github__list_repository_security_advisories",
                  "mcp__github__list_secret_scanning_alerts",
                  "mcp__github__list_starred_repositories",
                  "mcp__github__list_sub_issues",
                  "mcp__github__list_tags",
                  "mcp__github__list_workflow_jobs",
                  "mcp__github__list_workflow_run_artifacts",
                  "mcp__github__list_workflow_runs",
                  "mcp__github__list_workflows",
                  "mcp__github__manage_notification_subscription",
                  "mcp__github__manage_repository_notification_subscription",
                  "mcp__github__mark_all_notifications_read",
                  "mcp__github__merge_pull_request",
                  "mcp__github__push_files",
                  "mcp__github__remove_sub_issue",
                  "mcp__github__reprioritize_sub_issue",
                  "mcp__github__request_copilot_review",
                  "mcp__github__rerun_failed_jobs",
                  "mcp__github__rerun_workflow_run",
                  "mcp__github__run_workflow",
                  "mcp__github__search_code",
                  "mcp__github__search_issues",
                  "mcp__github__search_orgs",
                  "mcp__github__search_pull_requests",
                  "mcp__github__search_repositories",
                  "mcp__github__search_users",
                  "mcp__github__star_repository",
                  "mcp__github__submit_pending_pull_request_review",
                  "mcp__github__unstar_repository",
                  "mcp__github__update_gist",
                  "mcp__github__update_issue",
                  "mcp__github__update_pull_request",
                  "mcp__github__update_pull_request_branch"
                ]
              },
              "model": "opus",
              "env": {
                "_GH_PAGER_COMMENT": "disable gh from being able to treat the terminal as interactive",
                "GH_PAGER": "cat",
                "GH_PROMPT_DISABLED": "true",
                "_GH_BROWSER_COMMENT": "what app to open URLs with, since claude is not interactive, we use curl to pipe it to stdout",
                "GH_BROWSER": "curl",
                "CLAUDE_CODE_REMOTE": "true"
              }
            }
        with:
          # Claude API authentication with priority: CLAUDE_API_KEY > ANTHROPIC_API_KEY > CLAUDE_CODE_OAUTH_TOKEN
          anthropic_api_key: ${{
            secrets.CLAUDE_API_KEY && secrets.CLAUDE_API_KEY ||
            ( secrets.ANTHROPIC_API_KEY && secrets.ANTHROPIC_API_KEY || '' ) }}
          claude_code_oauth_token: ${{
            secrets.CLAUDE_API_KEY && '' ||
            ( secrets.ANTHROPIC_API_KEY && '' || secrets.CLAUDE_CODE_OAUTH_TOKEN ) }}
          # Bot identity for git commits (from GitHub App auth)
          bot_id: ${{ steps.auth.outputs.user-id }}
          bot_name: '@claude via ${{ steps.auth.outputs.user-name }}'
          allowed_bots: 'github-actions[bot],automation-nsheaps[bot]'
          github_token: ${{ steps.auth.outputs.token }}
          # Prompt extracted from repository_dispatch payload
          prompt: ${{ steps.prompt.outputs.prompt }}
          # Enable full output for debugging
          show_full_output: true
          additional_permissions: |
            actions: read
          settings: ${{ env.INPUT_SETTINGS_JSON }}
          # MCP servers and allowed tools for GitHub integration
          claude_args: >-
            --allowedTools
            mcp__github__*,
            mcp__github_comment__update_claude_comment,
            mcp__github_ci__get_ci_status,
            mcp__github_ci__get_workflow_run_details,
            mcp__github_ci__download_job_log
