# AGENTS.md - Repository Context for AI Assistants

> **⚠️ IMPORTANT FOR AI AGENTS**: This file provides critical context about the repository structure and conventions. **You MUST update this file after completing any task that makes significant changes to the repository.** Keeping this file current ensures accurate assistance for future tasks.

## Repository Overview

**Name**: Claude Code Plugin Marketplace
**Purpose**: A curated collection of high-quality plugins for Claude Code, focusing on git automation and intelligent development workflows
**Owner**: @nsheaps
**Repository**: https://github.com/nsheaps/.ai

## Quick Summary

This is a plugin marketplace for Claude Code that:
- Hosts reusable plugins (slash commands and agent skills)
- Automates plugin versioning and distribution
- Provides CI/CD workflows for quality assurance
- Offers Homebrew-based installation

## Directory Structure

```
.
├── .claude-plugin/
│   ├── marketplace.json      # Main marketplace metadata (auto-updated by CI)
│   └── plugin.json          # Root plugin configuration
│
├── .github/
│   ├── actions/             # Reusable composite actions
│   │   ├── lint-files/      # Multi-language linting (YAML, JSON, MD, TS, JS, PY)
│   │   ├── validate-plugins/    # Plugin structure validation
│   │   ├── update-marketplace/  # Marketplace sync automation
│   │   └── check-version-bump/  # Semantic versioning enforcement
│   │
│   ├── workflows/           # CI/CD pipelines
│   │   ├── ci.yaml         # Linting + validation (runs on PR + push to main)
│   │   ├── cd.yaml         # Version checks + marketplace updates
│   │   └── review.yaml     # AI-powered code review
│   │
│   ├── CODEOWNERS          # @nsheaps owns everything
│   └── pull_request_template.md
│
├── plugins/                # Plugin directory
│   ├── commit-command/     # Slash command: /commit
│   │   ├── .claude-plugin/
│   │   │   └── plugin.json  # Plugin metadata (version: 1.0.0)
│   │   ├── commands/
│   │   │   └── commit.md
│   │   └── README.md
│   │
│   └── commit-skill/       # Agent skill: smart-commit
│       ├── .claude-plugin/
│       │   └── plugin.json  # Plugin metadata (version: 1.0.0)
│       ├── skills/
│       │   └── smart-commit/
│       │       └── SKILL.md
│       └── README.md
│
├── docs/
│   └── todo.md            # Project roadmap and future tasks
│
├── Formula/               # Homebrew formula (auto-generated by CD workflow)
│   └── claude-plugin-marketplace.rb
│
├── renovate.json          # Dependency automation (extends nsheaps/renovate-config)
├── README.md              # User-facing documentation
└── AGENTS.md              # This file (AI context)
```

## Key Files & Their Purpose

### Marketplace Files
- **.claude-plugin/marketplace.json**: Central registry of all plugins. **Auto-updated by CD workflow** - DO NOT manually edit unless necessary
- **plugins/*/​.claude-plugin/plugin.json**: Individual plugin metadata including version numbers

### CI/CD Workflows

#### ci.yaml (Continuous Integration)
- **Triggers**: PR to any branch, push to main, manual dispatch
- **Jobs**:
  - `lint`: Multi-language linting with auto-fix
    - Runs linters for YAML, JSON, Markdown, TypeScript, JavaScript, Python
    - Auto-fixes issues and pushes back to branch
    - Removes old lint error comments on PRs
    - Reruns after fixes to capture unfixable errors
    - Comments unfixable errors on PR using peter-evans actions
    - **ALWAYS FAILS** if any lint errors found (even after auto-fix)
  - `validate`: Validates plugin structure and marketplace consistency

#### cd.yaml (Continuous Deployment)
- **Triggers**: Push to main or PR with `plugins/**` changes
- **Jobs**:
  - `check-version-bump`: Enforces semantic versioning in PRs
    - Uses `svu` utility for version suggestions
    - **FAILS** if plugin modified without version bump
    - Every plugin change requires a version bump - no exceptions
  - `update-marketplace`: Updates marketplace.json on main
    - Syncs plugin versions from plugin.json files
    - Commits with `[skip ci]` marker
    - Generates Homebrew formula for distribution

#### review.yaml (Code Review)
- **Triggers**: PR opened/synchronized/reopened
- **Jobs**: AI-powered code review using Claude Code

### Important Conventions

1. **Version Bumps**: REQUIRED for every plugin change
   - Patch (x.y.Z): Bug fixes, non-breaking changes
   - Minor (x.Y.0): New features, backwards compatible
   - Major (X.0.0): Breaking changes

2. **Commit Messages**: Use conventional commits format
   - `feat:` - New features
   - `fix:` - Bug fixes
   - `chore:` - Maintenance tasks
   - `docs:` - Documentation updates
   - Append `[skip ci]` to skip CI workflows when needed

3. **Git Pushes**: All workflows use retry logic (4 attempts, exponential backoff: 2s, 4s, 8s, 16s)

4. **Secrets**:
   - `GITHUB_PAT_TOKEN_NSHEAPS`: Personal access token for authenticated pushes
   - `ANTHROPIC_API_KEY`: For Claude Code review workflow

## Plugin Development Guidelines

### Technology Preferences

**For compiled binaries and complex plugins:**
- ✅ **Prefer Bun** as the runtime (fast, TypeScript-native, all-in-one tooling)
- ✅ **Prefer TypeScript** for type safety and better developer experience
- ✅ Use `bun build --compile` for standalone executables
- ✅ TypeScript provides better IDE support and catches errors early

**Why Bun + TypeScript:**
- Single tool for package management, building, testing, and running
- Native TypeScript support (no transpilation step needed)
- Fast startup and execution times
- Can compile to standalone binaries with `bun build --compile`
- Better than Node.js for CLI tools and automation scripts

### Adding a New Plugin

1. Create plugin directory: `plugins/plugin-name/`
2. Add `.claude-plugin/plugin.json` with metadata
3. Add plugin content (commands/ or skills/)
4. For complex plugins: Use TypeScript + Bun for compiled binaries
5. Add README.md with documentation
6. **Bump version** in plugin.json (start at 1.0.0)
7. Create PR - CI will validate structure
8. On merge to main, CD workflow updates marketplace.json automatically

### Plugin Structure Requirements

Every plugin must have:
- `.claude-plugin/plugin.json` with required fields:
  - `name`: Plugin identifier (lowercase, hyphen-separated)
  - `version`: Semver version (x.y.z)
  - `description`: Clear, concise description
  - `author`: Name and email
- README.md with usage instructions
- Either `commands/` or `skills/` directory (or both)

### Version Management

- **In PRs**: CD workflow checks that plugin versions are bumped
- **On main**: CD workflow syncs versions to marketplace.json
- **Never** manually edit marketplace.json versions - they're auto-synced from plugin.json files

## Current Plugins

### commit-command (v1.0.0)
- **Type**: Slash command
- **Usage**: `/commit [optional prefix]`
- **Purpose**: AI-generated commit messages matching repo conventions
- **Location**: plugins/commit-command/

### commit-skill (v1.0.0)
- **Type**: Agent skill
- **Activation**: Auto-activates during development tasks
- **Purpose**: Intelligent commit analysis and creation
- **Location**: plugins/commit-skill/

## Workflow Behavior

### When You Open a PR
1. CI lints all files, auto-fixes issues, pushes fixes
2. CI validates plugin structure
3. CD checks version bumps if plugins changed
4. Review workflow runs Claude Code review
5. Lint errors commented on PR if unfixable

### When You Push to Main
1. CI runs validation
2. CD updates marketplace.json if plugins changed
3. CD generates/updates Homebrew formula
4. Both commit with `[skip ci]` to avoid loops

## Renovate Configuration

- Extends: `github>nsheaps/renovate-config`
- Handles dependency updates automatically
- Configuration in: `renovate.json`

## Code Ownership

All files owned by: **@nsheaps** (see .github/CODEOWNERS)

## Common Tasks & How to Handle Them

### Adding/Modifying a Plugin
1. ✅ Make changes in `plugins/plugin-name/`
2. ✅ Bump version in `plugins/plugin-name/.claude-plugin/plugin.json`
3. ✅ Update plugin README if needed
4. ✅ **Update this AGENTS.md file** with changes
5. ✅ Commit with conventional commit format
6. ❌ DO NOT manually update marketplace.json (auto-synced)

### Updating CI/CD Workflows
1. ✅ Modify workflow files in `.github/workflows/`
2. ✅ Test workflow changes
3. ✅ **Update this AGENTS.md file** with workflow behavior changes
4. ✅ Update docs/todo.md if adding new tasks

### Adding Documentation
1. ✅ Update README.md for user-facing docs
2. ✅ Update relevant plugin READMEs
3. ✅ **Update this AGENTS.md file** if structure changes
4. ✅ Add to docs/ for internal documentation

### Fixing Linting Issues
- Let CI auto-fix when possible
- If CI comments unfixable errors, address manually
- Run linters locally before pushing: `yamllint`, `prettier`, `markdownlint-cli2`, `eslint`, `black`, `flake8`

## AI Agent Responsibilities

### CRITICAL: Keep This File Updated

**After completing ANY task that changes the repository, you MUST:**

1. ✅ Review what changed (files, structure, workflows, plugins)
2. ✅ Update this AGENTS.md file to reflect changes
3. ✅ Update version numbers in "Current Plugins" section if plugins changed
4. ✅ Update "Directory Structure" if files/folders added/removed
5. ✅ Update "Workflow Behavior" if CI/CD changed
6. ✅ Add new sections for new major features
7. ✅ Include the AGENTS.md update in the same commit as your changes

### Why This Matters

This file serves as the **source of truth** for AI agents working on this repository. An outdated AGENTS.md leads to:
- ❌ Incorrect assumptions about repository structure
- ❌ Missed validation requirements
- ❌ Wrong workflow expectations
- ❌ Confusion about plugin versions and state

**When in doubt, update AGENTS.md.** It's better to over-document than under-document.

## Recent Major Changes

### 2025-12-13: CI/CD Workflows Added
- Created comprehensive GitHub Actions workflows (ci, cd, review)
- Added composite actions for linting, validation, version checking
- Implemented auto-fix for lint issues with PR commenting
- Added Homebrew formula auto-generation
- Set up Renovate for dependency management
- Created CODEOWNERS and PR template

### 2025-12-13: Initial Marketplace Setup
- Created marketplace structure
- Added commit-command plugin (v1.0.0)
- Added commit-skill plugin (v1.0.0)
- Set up basic plugin metadata system

## Future Roadmap

See `docs/todo.md` for detailed roadmap. Key items:
- Claude Code Enterprise Settings Brew formula
- Additional plugins (auto-test-runner, pr-description-generator, etc.)
- Plugin marketplace enhancements
- Developer tooling improvements

---

**Last Updated**: 2025-12-13
**Last Updated By**: AI Agent (Claude)
**Update Reason**: Initial creation with comprehensive repository overview

> **Remember**: Update this file after every significant change to the repository!
