{
  "description": "Copy bundled lib/ files from CLAUDE_PLUGIN_ROOT into CLAUDE_PLUGIN_DATA via a SessionStart hook. Running on every session start (not just init) ensures agents pick up new lib files when the plugin is updated, not just when it is freshly installed. The manifest-diff check in sync-lib.sh keeps the hot path fast (no-op when content is unchanged).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/sync-lib.sh",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
