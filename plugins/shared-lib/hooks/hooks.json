{
  "description": "Copy bundled lib/ files from CLAUDE_PLUGIN_ROOT into CLAUDE_PLUGIN_DATA via a Setup{trigger:init} hook. The launcher fires a `claude --init-only` pre-pass before the interactive session starts so the libs are on disk before any dependent plugin's SessionStart hook runs.",
  "hooks": {
    "Setup": [
      {
        "matcher": "init",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/sync-lib.sh",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
