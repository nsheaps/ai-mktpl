{
  "name": "watch-command-lsp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --target=node --outdir=./bin --entry-naming=[name].mjs src/index.ts",
    "build:compile": "bun build --compile --outfile=./bin/watch-command-lsp src/index.ts",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
