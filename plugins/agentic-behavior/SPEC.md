# Plugin: agentic-behavior

**Purpose**: Skills for configuring Claude Code, behavior correction, memory tracking, autonomy rules, and time-context awareness.

## Skills

- `brain` — Git-backed memory and prompt tracking with self-checking reminders. Auto-saves prompts, syncs memory to git, and...
- `claude-code` — - Reference material for Claude Code internals — the on-disk layout under ~/.claude and project-scope .claude, the...
- `continue-work` — - Session recovery skill. Use on startup, after compaction, or when resuming work after any interruption. Restores...
- `correct-behavior` — - Use this skill when the user says you did something wrong, made a mistake, or wants to correct your behavior. Trigger...
- `exit` — Gracefully exit the Claude Code session by sending SIGINT to the Claude process. Works for local CLI sessions and...
- `incident-tracker` — - Use this skill when the user wants to log a behavioral incident, document a correction in a structured incident...
- `restart` — Restart the Claude Code session by gracefully exiting so the launcher loop restarts it. Use when you need to pick up...
- `spec-management` — This skill should be used when creating, updating, or reviewing specifications during the PR process. Trigger phrases:...
- `time-context` — Detect time-referencing language in user prompts and investigate history before answering. Auto-recall when users say...

## Rules

- `acknowledge-before-working` — When you receive a message from the handler requesting work, you MUST acknowledge it before starting.
- `autonomy` — <!-- handler feedback #41 -->
- `communication-discipline` — Rules for keeping handler-facing communication concise and plan-doc-first.
- `dont-relay-to-visible-parties` — When multiple parties share a conversation or channel, **don't relay information from one member to another** —...
- `message-link-formatting` — \*\*Every reference to a PR, issue, commit, or external resource in a Discord or Telegram message MUST be a clickable...
- `thread-history` — When posting in a Discord thread or channel where prior messages exist, you MUST acknowledge and reference the existing...
- `work-tracking` — Cross-cutting rules for how tasks, PRs, milestones, and project management artifacts relate to each other.

## Hooks

- `PreToolUse` (`prompt`) — Agentic behavior hooks: saves prompts to history on submit, syncs rules via symlink, auto-syncs memory to git repo on session start and task completion, enforces sub-agent output-to-file pattern
- `UserPromptSubmit` (`bash`) — Agentic behavior hooks: saves prompts to history on submit, syncs rules via symlink, auto-syncs memory to git repo on session start and task completion, enforces sub-agent output-to-file pattern
- `SessionStart` (`bash`) — Agentic behavior hooks: saves prompts to history on submit, syncs rules via symlink, auto-syncs memory to git repo on session start and task completion, enforces sub-agent output-to-file pattern
- `TaskCompleted` (`bash`) — Agentic behavior hooks: saves prompts to history on submit, syncs rules via symlink, auto-syncs memory to git repo on session start and task completion, enforces sub-agent output-to-file pattern
