# Reference Implementations

Archived implementations preserved for future reference.

## mcp-token-refresh-server.sh.bak

A bash-based MCP stdio server (JSON-RPC 2.0) for GitHub App token refresh.
Replaced by hook-based approach (SessionStart + PreToolUse) for better integration
with Claude Code's tool lifecycle. Preserved here as a reference for writing
bash MCP servers.
