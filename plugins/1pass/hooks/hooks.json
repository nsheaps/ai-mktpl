{
  "description": "Install 1Password CLI/op-exec, inject secrets on session start, and redact secrets from tool outputs",
  "hooks": {
    "Setup": [
      {
        "matcher": "init",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/op-exec-env-setup.sh",
            "timeout": 120
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/install-op.sh",
            "timeout": 120
          },
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/op-exec-env.sh",
            "timeout": 60
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "_note": "This entry did not fire in Claude Code CLI due to upstream bug https://github.com/anthropics/claude-code/issues/6305 (empirically confirmed v2.1.128 CLI, 2026-05-19). Re-tested on v2.1.231 (2026-08-14): PostToolUse now fires from a plugin hooks.json, so this entry is live. Kept only as provenance for the settings.json workaround this plugin may still carry; see plugins/CLAUDE.md and https://github.com/nsheaps/ai-mktpl/issues/753.",
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/redact-secrets.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
