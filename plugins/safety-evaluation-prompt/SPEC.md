# Plugin: safety-evaluation-prompt

**Purpose**: An AI-powered pre-tool-call safety evaluation plugin for Claude Code that uses prompt-style hooks for inline security assessment.

## Hooks
- `PreToolUse` (`prompt`) — Pre-tool-call safety evaluation using prompt-style hooks for inline AI evaluation
