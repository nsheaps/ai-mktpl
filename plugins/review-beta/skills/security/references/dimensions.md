# Dimensions — `security` family

Extracted verbatim from the canonical review taxonomy (internal source, not shipped with
this plugin: `.ai-agent-jack/docs/research/review-taxonomy/taxonomy.md`, "Family: security").
**The dimension table below is self-contained — it is the authority for this aspect.**
Column meanings, the severity rubric and the cross-family de-duplication ledger live only in
that source, so a reader without it loses the ledger, not the dimensions.

## Contents

1. [Dimension table](#family-security)
2. [References](#references)

## Family: security

Application security, personal-data handling, and the software supply chain. **24 dimensions.**

| id                                  | name                                      | the question                                                                                                                              | severity | scope | tier      | agent      | automatable-with                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | residual                                                                               |
| :---------------------------------- | :---------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------- | :------- | :---- | :-------- | :--------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------- |
| `sec-injection-interpreter`         | Interpreter injection                     | Does untrusted input reach a query or command interpreter without parameterization or a safe API?                                         | blocker  | diff  | high      | adjudicate | Semgrep taint mode (source→sink, parameterized API as sanitizer); CodeQL `security-extended`[^semgrep][^codeql][^owasp-top10]                                                                                                                                                                                                                                                                                                                                                            | Whether the flagged source is genuinely attacker-reachable                             |
| `sec-xss-render-sink`               | Browser render-sink injection (XSS)       | Does untrusted data reach an HTML/JS execution sink without context-correct encoding or sanitization?                                     | blocker  | diff  | high      | adjudicate | Semgrep taint rules, CodeQL JS/TS queries, `react/no-danger` and framework escape-hatch lints[^semgrep][^codeql][^owasp-xss]                                                                                                                                                                                                                                                                                                                                                             | Encoding context of the sink                                                           |
| `sec-path-traversal`                | Path & archive traversal                  | Can an attacker-controlled path component resolve outside the intended directory, on read, write, extract or serve?                       | blocker  | diff  | high      | adjudicate | Semgrep taint to filesystem sinks with `realpath`+containment as sanitizer; CodeQL path-injection; Zip Slip fixture test[^semgrep][^codeql][^cwe22]                                                                                                                                                                                                                                                                                                                                      | Symlink and case-folding edge cases on the target platform                             |
| `sec-deserialization`               | Unsafe deserialization                    | Is untrusted or unauthenticated data passed to a deserializer that reconstructs arbitrary types?                                          | blocker  | diff  | very-high | adjudicate | Near-pure grep over a finite dangerous-API set per language; Semgrep + CodeQL unsafe-deserialization queries[^semgrep][^codeql][^owasp-deser]                                                                                                                                                                                                                                                                                                                                            | Whether the input is authenticated upstream                                            |
| `sec-ssrf-egress`                   | SSRF / egress target control              | Can a caller steer a server-side request to an arbitrary destination?                                                                     | blocker  | diff  | high      | adjudicate | Semgrep taint to HTTP clients; deterministic script asserting required CIDRs in the blocklist; redirect/scheme config grep[^semgrep][^owasp-ssrf]                                                                                                                                                                                                                                                                                                                                        | Whether the allowlist is complete for the deployment's metadata endpoints              |
| `sec-csrf-state-change`             | CSRF on state-changing routes             | Are cookie-authenticated state changes protected against cross-site submission?                                                           | major    | diff  | high      | adjudicate | Route-table + middleware-chain extraction; `@csrf_exempt` grep; cookie-attribute grep; credentialed-wildcard-CORS Semgrep; token-less cross-origin POST integration test[^owasp-csrf][^semgrep]                                                                                                                                                                                                                                                                                          | Whether an exempt route is genuinely non-cookie-authenticated                          |
| `sec-authz-enforcement`             | Authorization enforcement (object level)  | Is every non-public resource deny-by-default, ownership-checked, and enforced server-side?                                                | blocker  | diff  | high      | adjudicate | `schemathesis run --checks ignored_auth` re-issues every spec operation with credentials removed or corrupted and fails on any 2xx[^schemathesis]; OWASP ZAP Automation Framework `accessControl` job for cookie/session apps; route↔policy-map diff failing on unmapped routes; a required "user B reads user A's object → 403" test[^owasp-top10]                                                                                                                                      | Resources not described by the spec — itself detectable as route-table minus spec-path |
| `sec-field-level-exposure`          | Field-level over-posting & over-returning | Can a caller write a field they should not set, or read a field they should not see, on an object they are otherwise authorised to touch? | blocker  | diff  | high      | adjudicate | Semgrep for whole-body binding with no allowlist (`Object.assign(entity, req.body)`, `Model.objects.update(**data)`, `json.Unmarshal` into a persistence struct on a request path, Rails `permit!`)[^semgrep]; serializer/DTO diff — any new field in an OpenAPI/GraphQL response schema (`oasdiff`) whose backing column is classified non-public[^oasdiff]; required negative test ("user B sets `role` via PATCH → 403/ignored")[^owasp-top10]                                        | Whether an added response field is genuinely public                                    |
| `sec-resource-exhaustion-limits`    | Attacker-driven resource exhaustion       | Can an unauthenticated or low-privilege caller make this path consume unbounded CPU, memory, storage, connections or spend?               | blocker  | diff  | high      | adjudicate | `safe-regex` / `redos-detector` over regexes constructed from request data; route-table diff asserting every new route appears in the rate-limit/quota middleware map; body-parser/multipart `limit` config assertion; missing `LIMIT`/max-page-size on new list endpoints; GraphQL depth/complexity plugin presence; decompression-ratio guard on any new archive/`gzip` read (CWE-400/CWE-770)[^semgrep][^owasp-top10]                                                                 | Whether the declared limit is low enough for the deployment's capacity                 |
| `sec-authn-session`                 | Authentication & session identity         | Are credentials, session identifiers and token claims handled to standard?                                                                | blocker  | diff  | high      | adjudicate | Grep/AST: hash choice, JWT verify options, `Math.random` identifiers, `==` on secrets, session-in-URL; config check for default credentials, MFA, login rate limit[^owasp-top10][^owasp-asvs][^semgrep]                                                                                                                                                                                                                                                                                  | Session-fixation and step-up semantics of the flow                                     |
| `sec-crypto-primitives`             | Cryptographic primitive selection         | Are algorithms, modes, IVs, randomness and TLS validation non-deprecated and correctly parameterized?                                     | major    | diff  | very-high | adjudicate | Finite denylist of identifiers and config keys — Semgrep/CodeQL crypto packs; `sslyze`/`testssl.sh` against a deployed endpoint[^semgrep][^codeql][^owasp-top10]                                                                                                                                                                                                                                                                                                                         | Key-lifetime and nonce-reuse arguments the denylist cannot see                         |
| `sec-secret-material-exposure`      | Secret material exposure                  | Does any real secret value exist in source, git history, logs, build artifacts or a client bundle?                                        | blocker  | diff  | very-high | adjudicate | `trufflehog git file://. --results=verified --json` performs live verification against the issuing provider — **verified hits are reported with no agent involvement**[^trufflehog]. `gitleaks` (history + SARIF) and GitHub secret scanning with push protection supply the unverified candidate set; grep over the built bundle[^gitleaks][^gh-secret-scanning][^owasp-secrets]                                                                                                        | Unverified gitleaks hits only: fixture vs live credential                              |
| `sec-credential-scope-and-rotation` | Credential scope, rotation & blast radius | Is each credential this change introduces or widens scoped to the minimum it needs, rotatable, and revocable — with a named owner?        | major    | diff  | high      | adjudicate | IaC policy diff for wildcard `Action`/`Resource`/principal via the IAM rule packs in Checkov/KICS/Conftest[^conftest-opa]; static-cloud-key detection where an OIDC provider already exists in the same account; secret-registry lint requiring `owner` + `rotation_period` + `last_rotated` for every new key; DB grant diff                                                                                                                                                            | Whether a narrower scope actually satisfies the workload                               |
| `sec-personal-data-handling`        | Personal data collection & disclosure     | Does this change collect, store, transmit or expose personal data beyond what is classified, minimised and lawfully permitted?            | blocker  | diff  | partial   | judge      | A field-classification registry (`data-classification.yaml`) cross-joined with new migration columns, proto fields and GraphQL fields, failing on any unclassified addition; Semgrep taint from PII-named identifiers (`email\|ssn\|dob\|phone\|address\|ip_addr\|lat\|lng`) to logger, analytics, third-party-SDK and model-client sinks[^semgrep]; diff rule requiring a DPIA/privacy-review label when a `restricted`-classified field is added                                       | Lawful basis and minimisation argument for the new field or sink                       |
| `sec-data-retention-deletion`       | Retention bound & deletion coverage       | Does every new store of personal data have a retention bound and appear in the deletion/anonymisation path?                               | major    | diff  | high      | adjudicate | Set-difference of tables/buckets/topics created by migrations and IaC minus those enumerated in the deletion job and the retention policy config; TTL-attribute assertion on new DynamoDB/Redis entries and S3 lifecycle resources                                                                                                                                                                                                                                                       | Whether a derived store (cache, warehouse copy, backup) is in scope                    |
| `sec-audit-trail-completeness`      | Security audit trail                      | Does every new security-relevant or privileged action emit an attributable, tamper-evident audit record?                                  | major    | diff  | high      | adjudicate | Route/handler↔audit-emit map (reusing the route↔policy-map extraction), failing on any handler annotated `@RequiresRole`/`@AdminOnly` with no audit emit in its call graph; Semgrep for audit calls whose error is discarded or swallowed; config assertion that the audit sink is append-only and on a separate destination; required-fields lint on the audit event schema (actor, action, target, outcome, request id)[^semgrep][^owasp-top10]                                        | Whether a new action is security-relevant                                              |
| `sec-model-input-trust-boundary`    | Model prompt & tool-use trust boundary    | Does attacker-influenceable content reach a model whose output drives privileged tools, and is that path scoped and reviewed?             | major    | diff  | partial   | adjudicate | Semgrep taint from untrusted sources (webhook payload fields, issue/PR body, fetched HTTP body, fork-originated repo content) to prompt-construction sinks, and from model output to `exec`/`spawn`/file-write/HTTP sinks with no allowlist[^semgrep]; workflow assertion that any job invoking an agent on fork-originated content runs with no write `permissions:` and no `secrets: inherit`[^zizmor][^gh-securitylab-pwn]; MCP/tool manifest lint requiring declared scopes per tool | Whether the tool's scope is genuinely bounded                                          |
| `sec-sandbox-isolation-csp`         | Renderer sandbox & CSP                    | Is untrusted content rendered in an isolated context under a strict CSP, with no privileged API bridged in?                               | blocker  | diff  | high      | adjudicate | CSP-string parser asserting required directives / forbidden keywords; grep over `webPreferences`/`createWebviewPanel` options; `electronegativity`[^owasp-csp][^vscode-webview][^electron-security]                                                                                                                                                                                                                                                                                      | Whether a bridged API is genuinely safe to expose                                      |
| `sec-dep-source-trust`              | Dependency source trust                   | Can a package resolve to an attacker-controlled name, registry, or install script?                                                        | blocker  | diff  | very-high | adjudicate | `.npmrc`/`NuGet.config`/`pip.conf` scope→registry assertion, HTTPS-registry assertion, `npm audit signatures`, lifecycle-script allowlist, typosquat edit distance[^npm-install-integrity][^npm-provenance][^birsan]                                                                                                                                                                                                                                                                     | Whether a near-miss name is a real typosquat                                           |
| `sec-dep-vuln-maintenance`          | Known-vulnerable & unmaintained deps      | Does the resolved graph — transitive included — contain advisories or abandoned components?                                               | major    | diff  | very-high | adjudicate | `osv-scanner` (lockfiles, images, SBOMs, call analysis); registry API for last-publish/deprecated/archived; depcheck/knip for unused deps; Scorecard `Maintained`[^osv-scanner][^scorecard][^knip][^cwe1104]                                                                                                                                                                                                                                                                             | Reachability of the advisory in our call graph                                         |
| `sec-license-sbom`                  | License risk & SBOM completeness          | Is every component's license identified and policy-compliant, and does the build emit an accurate SBOM?                                   | major    | repo  | very-high | skip       | `osv-scanner` license scan, SPDX-identifier validation, `syft`/`cdxgen` SBOM diffed against the lockfile, `gh attestation verify`[^osv-scanner][^spdx][^spdx-licenses][^cyclonedx][^gh-artifact-attestations]                                                                                                                                                                                                                                                                            | none                                                                                   |
| `sec-ci-trigger-privilege`          | CI trigger & privilege boundary           | Does a workflow grant write tokens or secrets to a job that touches untrusted code?                                                       | blocker  | repo  | very-high | adjudicate | `zizmor` (`dangerous-triggers`, `excessive-permissions`, `artipacked`, `secrets-inherit`, `self-hosted-runner`, `bot-conditions`, `cache-poisoning`); Scorecard `Token-Permissions`/`Dangerous-Workflow`[^zizmor][^scorecard][^gh-actions-secure-use][^gh-securitylab-pwn][^owasp-cicd]                                                                                                                                                                                                  | Whether a flagged trigger genuinely handles untrusted code                             |
| `sec-ci-action-integrity`           | Third-party action/step integrity         | Is every external CI step pinned to an immutable, authentic reference?                                                                    | major    | repo  | very-high | skip       | `zizmor` `unpinned-uses`, `impostor-commit`, `ref-confusion`, `ref-version-mismatch`, `typosquat-uses`, `archived-uses`, `forbidden-uses`, `unpinned-images`; Scorecard `Pinned-Dependencies`[^zizmor][^scorecard]                                                                                                                                                                                                                                                                       | none                                                                                   |
| `sec-release-provenance`            | Release provenance & attestation          | Do published artifacts carry verifiable, non-forgeable build provenance produced only by the CI path?                                     | major    | repo  | high      | adjudicate | Parse the release workflow for an attestation step + required `permissions`; `--provenance` on publish; post-release `gh attestation verify` / `npm audit signatures` / Scorecard `Signed-Releases`[^gh-artifact-attestations][^npm-provenance][^slsa-build][^scorecard][^nist-ssdf]                                                                                                                                                                                                     | Whether the attested builder is the only publish path                                  |

Grounded in: [^owasp-top10][^owasp-asvs][^owasp-xss][^owasp-ssrf][^owasp-csrf][^owasp-deser][^owasp-csp][^owasp-secrets][^owasp-cicd][^cwe22][^cwe1104][^slsa-build][^nist-ssdf][^spdx][^spdx-licenses][^cyclonedx][^birsan][^semgrep][^codeql][^gitleaks][^trufflehog][^schemathesis][^gh-secret-scanning][^osv-scanner][^scorecard][^zizmor][^actionlint][^conftest-opa][^oasdiff][^npm-install-integrity][^npm-provenance][^gh-artifact-attestations][^gh-actions-secure-use][^gh-securitylab-pwn][^vscode-webview][^electron-security]

---

## References

[^actionlint]: rhysd, _actionlint_ — workflow syntax, expression types, action inputs, job graphs; shellcheck/pyflakes over `run:`. <https://github.com/rhysd/actionlint>

[^birsan]: Alex Birsan, _Dependency Confusion: How I Hacked Into Apple, Microsoft and Dozens of Other Companies_. <https://medium.com/@alex.birsan/dependency-confusion-4a5d60fec610>

[^codeql]: GitHub Docs — _CodeQL code scanning_ and _CodeQL query suites_ (`default`, `security-extended`). <https://docs.github.com/en/code-security/concepts/code-scanning/codeql/codeql-code-scanning> · <https://docs.github.com/en/code-security/concepts/code-scanning/codeql/codeql-query-suites>

[^conftest-opa]: Open Policy Agent / Conftest — policy over structured configuration; Checkov and KICS provide equivalent IaC and IAM policy scanning. <https://www.conftest.dev/>

[^cwe1104]: MITRE, _CWE-1104: Use of Unmaintained Third Party Components_. <https://cwe.mitre.org/data/definitions/1104.html>

[^cwe22]: MITRE, _CWE-22: Improper Limitation of a Pathname to a Restricted Directory ('Path Traversal')_. <https://cwe.mitre.org/data/definitions/22.html>

[^cyclonedx]: CycloneDX Specification Overview (ECMA-424, v1.7). <https://cyclonedx.org/specification/overview/>

[^electron-security]: Electron, _Security checklist_ — `nodeIntegration`, `contextIsolation`, `sandbox`, `webSecurity`, CSP, `allowpopups`, `shell.openExternal`. <https://www.electronjs.org/docs/latest/tutorial/security>

[^gh-actions-secure-use]: GitHub Docs, _Secure use reference for GitHub Actions_ — default read-only `GITHUB_TOKEN`, SHA pinning, env-var indirection for untrusted context, self-hosted runner caveats, secret masking; includes workflow `permissions` syntax. <https://docs.github.com/en/actions/reference/security/secure-use>

[^gh-artifact-attestations]: GitHub Docs, _Use artifact attestations to establish provenance for builds_ — required permissions; `gh attestation verify`. <https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations>

[^gh-secret-scanning]: GitHub Docs, _Secret scanning_ — full history, partner/generic/custom patterns, rotation as remediation. <https://docs.github.com/en/code-security/concepts/secret-security/secret-scanning>

[^gh-securitylab-pwn]: GitHub Security Lab, _Preventing pwn requests_ — `pull_request_target` + untrusted checkout; `workflow_run` split; label-gating is race-prone. <https://securitylab.github.com/resources/github-actions-preventing-pwn-requests/>

[^gitleaks]: gitleaks — regex + entropy secret scanning over history, directories and stdin; `--redact`, `--baseline-path`, SARIF. <https://github.com/gitleaks/gitleaks>

[^knip]: Knip — unused files, dependencies, exports, exported types, unlisted dependencies. <https://knip.dev/>

[^nist-ssdf]: NIST, _Secure Software Development Framework (SSDF) v1.1_, SP 800-218 (practice groups PO/PS/PW/RV). <https://csrc.nist.gov/pubs/sp/800/218/final>

[^npm-install-integrity]: npm Docs — `npm ci` <https://docs.npmjs.com/cli/v11/commands/npm-ci>; `ignore-scripts` <https://docs.npmjs.com/cli/v11/using-npm/config#ignore-scripts>; lifecycle scripts <https://docs.npmjs.com/cli/v11/using-npm/scripts>; `.npmrc` scope-to-registry mapping <https://docs.npmjs.com/cli/v11/configuring-npm/npmrc>

[^npm-provenance]: npm Docs, _Generating provenance statements_ — `--provenance`, `id-token: write`, Sigstore, `npm audit signatures`. <https://docs.npmjs.com/generating-provenance-statements>

[^oasdiff]: oasdiff — OpenAPI diff with a `breaking` command and a GitHub Action. <https://github.com/Tufin/oasdiff>

[^osv-scanner]: Google, _OSV-Scanner_ — lockfiles, container images, SBOMs, offline mode, call analysis, license scanning. <https://github.com/google/osv-scanner>

[^owasp-asvs]: OWASP, _Application Security Verification Standard (ASVS) v5.0.0_. <https://owasp.org/www-project-application-security-verification-standard/>

[^owasp-cicd]: OWASP, _Top 10 CI/CD Security Risks_. <https://owasp.org/www-project-top-10-ci-cd-security-risks/>

[^owasp-csp]: OWASP, _Content Security Policy Cheat Sheet_. <https://cheatsheetseries.owasp.org/cheatsheets/Content_Security_Policy_Cheat_Sheet.html>

[^owasp-csrf]: OWASP, _Cross-Site Request Forgery Prevention Cheat Sheet_. <https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html>

[^owasp-deser]: OWASP, _Deserialization Cheat Sheet_. <https://cheatsheetseries.owasp.org/cheatsheets/Deserialization_Cheat_Sheet.html>

[^owasp-secrets]: OWASP, _Secrets Management Cheat Sheet_. <https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html>

[^owasp-ssrf]: OWASP, _Server Side Request Forgery Prevention Cheat Sheet_. <https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html>

[^owasp-top10]: OWASP Top 10:2025 — index <https://owasp.org/Top10/2025/>; A01 Broken Access Control <https://owasp.org/Top10/2025/A01_2025-Broken_Access_Control/>; A02 Security Misconfiguration <https://owasp.org/Top10/2025/A02_2025-Security_Misconfiguration/>; A03 Software Supply Chain Failures <https://owasp.org/Top10/2025/A03_2025-Software_Supply_Chain_Failures/>; A04 Cryptographic Failures <https://owasp.org/Top10/2025/A04_2025-Cryptographic_Failures/>; A05 Injection <https://owasp.org/Top10/2025/A05_2025-Injection/>; A07 Authentication Failures <https://owasp.org/Top10/2025/A07_2025-Authentication_Failures/>; A08 Software or Data Integrity Failures <https://owasp.org/Top10/2025/A08_2025-Software_or_Data_Integrity_Failures/>; Top 10:2021 (A10 SSRF) <https://owasp.org/Top10/2021/>

[^owasp-xss]: OWASP, _Cross Site Scripting Prevention Cheat Sheet_. <https://cheatsheetseries.owasp.org/cheatsheets/Cross_Site_Scripting_Prevention_Cheat_Sheet.html>

[^schemathesis]: Schemathesis — property-based API testing from an OpenAPI/GraphQL schema; the `ignored_auth` check re-issues each operation without credentials and with invalid credentials and fails when a protected operation returns 2xx. <https://schemathesis.readthedocs.io/en/stable/reference/checks/>

[^scorecard]: OpenSSF Scorecard — `Pinned-Dependencies`, `Vulnerabilities`, `Maintained`, `Dependency-Update-Tool`, `Token-Permissions`, `Dangerous-Workflow`, `Signed-Releases`, `Packaging`. <https://securityscorecards.dev/>

[^semgrep]: Semgrep — _Writing rules_ (`patterns`, `pattern-not`, `pattern-inside`, metavariables) and _Taint mode_ (sources, sinks, sanitizers, propagators). <https://docs.semgrep.dev/writing-rules/overview> · <https://docs.semgrep.dev/writing-rules/data-flow/taint-mode/overview>

[^slsa-build]: SLSA v1.0 — _Security levels_ <https://slsa.dev/spec/v1.0/levels> and _Build track requirements_ <https://slsa.dev/spec/v1.0/requirements>

[^spdx-licenses]: SPDX License List. <https://spdx.org/licenses/>

[^spdx]: SPDX (ISO/IEC 5962:2021). <https://spdx.dev/>

[^trufflehog]: TruffleHog — programmatic verification of detected credentials against the issuing provider; `--results=verified` reports only credentials that actually authenticate. <https://github.com/trufflesecurity/trufflehog>

[^vscode-webview]: Visual Studio Code, _Webview API extension guide_ — required CSP, `localResourceRoots`, `asWebviewUri`, never rely on sanitization alone. <https://code.visualstudio.com/api/extension-guides/webview>

[^zizmor]: zizmor — static analysis for GitHub Actions, and its audit rule reference. <https://github.com/zizmorcore/zizmor> · <https://docs.zizmor.sh/audits/>
