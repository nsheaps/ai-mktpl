# Dimensions — `org-fit` family

Extracted verbatim from the canonical review taxonomy (internal source, not shipped with
this plugin: `.ai-agent-jack/docs/research/review-taxonomy/taxonomy.md`, "Family: org-fit").
**The dimension table below is self-contained — it is the authority for this aspect.**
Column meanings, the severity rubric and the cross-family de-duplication ledger live only in
that source, so a reader without it loses the ledger, not the dimensions.

## Contents

1. [Dimension table](#family-org-fit)
2. [References](#references)

## Family: org-fit

Does this change fit the organization it lands in — reuse of org assets, conformance to the repo's
own stated rules and interface conventions, the accessibility that automated scanning cannot see,
internationalization, performance budgets, and cross-platform portability. **16 dimensions.**

| id                                         | name                                           | the question                                                                                                                                                                                 | severity | scope | tier      | agent      | automatable-with                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | residual                                                                                             |
| :----------------------------------------- | :--------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------- | :---- | :-------- | :--------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :--------------------------------------------------------------------------------------------------- |
| `org-fit-shared-library-reuse`             | Reinvented shared capability                   | Does this hand-roll something an existing shared library, platform capability or stdlib already provides?                                                                                    | minor    | diff  | high      | adjudicate | **Not a clone detector** — reinvention shares no tokens with the library it should have used. A checked-in `.review/blessed-capabilities.yaml` maps capability → blessed package → a Semgrep rule matching the hand-rolled _shape_ (`retry` → a `for` loop containing `sleep` and a caught exception; `slugify` → chained `toLowerCase().replace(/[^a-z0-9]+/g,'-')`; `parseDuration` → a regex over `\d+(ms\|s\|m\|h)`), plus a manifest check that the blessed package is a declared dependency. Each entry is written once and costs zero tokens thereafter. `gh search code` is optional enrichment, never the decider[^semgrep][^swe-ch16][^innersource-common-req]                                                                                                              | Whether the blessed library actually covers this case                                                |
| `org-fit-ci-pipeline-reuse`                | Copy-pasted CI instead of shared workflow      | Does this inline CI steps the org already publishes as a reusable workflow or composite action?                                                                                              | major    | repo  | very-high | skip       | Parse all workflow YAML, hash normalised step sequences, flag blocks repeated ≥2× or matching a known org reusable-workflow signature; assert `extends` on shared config presets[^gh-actions-avoid-dup][^renovate-presets]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | none                                                                                                 |
| `org-fit-golden-path-divergence`           | Off the paved road                             | Does this provision, deploy or scaffold outside the platform's golden path?                                                                                                                  | major    | diff  | very-high | adjudicate | Conftest/OPA or Checkov/KICS policy over rendered manifests; base-image allowlist regex over `FROM`; required-file presence against the org template manifest[^conftest-opa][^cncf-platforms][^cncf-maturity]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Whether the divergence is a justified exception                                                      |
| `org-fit-generated-artifact-drift`         | Stale generated artifact                       | Are checked-in generated files regenerated from the source this diff changed?                                                                                                                | major    | diff  | very-high | skip       | Run the repo's regeneration target then `git diff --exit-code`; `go mod tidy -diff`; `make verify`. A repo with generated artifacts and no such target is itself the finding[^go-mod-tidy-diff][^k8s-make-update]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | none                                                                                                 |
| `org-fit-dependency-version-divergence`    | Divergent / redundant dependency               | Does this add a dependency at a version the org has already settled, or a second library for a job an existing dependency already does?                                                      | major    | diff  | very-high | adjudicate | `npm ls <pkg>` / `syncpack list-mismatches` / `go mod graph` / `cargo tree -d` for duplicate resolution; a curated "one blessed library per job" list turns functional redundancy into a lookup[^syncpack][^renovate-presets]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Whether the second library is genuinely needed                                                       |
| `org-fit-repo-stated-rules`                | Repo's own written rules violated              | Does the change break a rule this repository explicitly wrote down?                                                                                                                          | major    | diff  | high      | adjudicate | **The rules corpus carries the burden, not the reviewer.** Every rule file (or every H2 within it) must carry front-matter `enforced-by: <tool>#<rule-id>` or `enforced-by: none`. The battery (a) asserts each `enforced-by` target exists in the tool config — a dangling-reference check — and (b) hands the agent _only_ the `enforced-by: none` subset, parsed once into `.review/rules-index.yaml`. A companion rule fails when the `enforced-by: none` subset exceeds N rules, operationalising rule 3[^swe-ch20]                                                                                                                                                                                                                                                              | The `enforced-by: none` subset only — and each finding must emit a "make this a lint rule" follow-up |
| `org-fit-interface-convention-conformance` | Public interface off-convention & misuse-prone | Do new public interfaces follow the org's documented conventions, and are their signatures hard to misuse?                                                                                   | major    | diff  | high      | adjudicate | `api-linter --set-exit-status`, `buf lint`, Spectral for proto/OpenAPI; name-shape regex derived from existing siblings for CLI flags, metric names and log fields[^api-linter][^aip180]. Ergonomics absorbed from the deleted `design-interface-shape`, each with a shipped linter: boolean-trap parameters → Ruff `FBT001`/`FBT002`/`FBT003`, `clippy::fn_params_excessive_bools`, SonarQube S2301; argument count → ESLint `max-params`, `clippy::too_many_arguments`, PMD `ExcessiveParameterList`, SonarQube S107[^eslint-max-params][^sonar-metrics]; adjacent same-type (swappable) parameters → SonarQube S2234; nullable-success returns → typescript-eslint `no-unnecessary-condition` plus a Semgrep pattern for `(T \| null, error)` tuples[^typescript-eslint][^semgrep] | Whether the house convention has a documented exception for this shape                               |
| `org-fit-a11y-semantics-and-keyboard`      | Custom widget semantics & keyboard operability | Does this re-implement a native control with ARIA, and if so is it fully keyboard-operable with correct focus management and correct announcements?                                          | major    | diff  | high      | adjudicate | `@guidepup/virtual-screen-reader` asserts the exact announced phrase sequence in a Vitest/Playwright test (`expect(await virtual.spokenPhraseLog()).toEqual([…])`), and `@guidepup/guidepup` drives real VoiceOver/NVDA in CI[^guidepup]; a Playwright `Tab` loop with `expect(locator).toBeFocused()` and `aria-activedescendant` assertions; `jsx-a11y` interaction rules narrow the candidate set. **Diff predicate**: a diff adding `role=` on a non-semantic element requires a guidepup spec[^jsx-a11y][^using-aria][^aria-apg]                                                                                                                                                                                                                                                 | Whether the announced string is the right one for the interaction                                    |
| `org-fit-a11y-media-alternatives`          | Media alternatives (WCAG 1.2.x)                | Does new time-based or non-text media ship with the alternative its type requires?                                                                                                           | major    | diff  | very-high | adjudicate | Lint requiring a `<track kind="captions">` or a linked transcript for every new `<video>`/`<audio>`/embed; asset-manifest set-difference against the alternatives directory[^wcag22]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Whether an existing transcript is accurate and complete                                              |
| `org-fit-a11y-adaptive-presentation`       | Zoom, reflow, motion & target size             | Does the surface survive 200% zoom, 320 px reflow, forced colors, reduced motion, and a 24 px target minimum?                                                                                | major    | diff  | high      | adjudicate | Playwright viewport/zoom matrix with `emulateMedia({ reducedMotion, forcedColors })` plus screenshot and horizontal-overflow assertions; the axe target-size rule[^axe-core]; a stylelint ban on `animation`/`transition` in new component CSS with no `prefers-reduced-motion` guard (WCAG 1.4.4/1.4.10/2.3.3/2.5.8)[^wcag22]                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Whether a clipped element is genuinely unreachable                                                   |
| `org-fit-a11y-forms-and-status`            | Form labelling, errors & status messages       | Are new inputs programmatically labelled, are errors identified and described in text, and is async state announced?                                                                         | major    | diff  | high      | adjudicate | `jsx-a11y` label rules plus axe `form-field-multiple-labels` and `aria-*` rules[^jsx-a11y][^axe-core]; a scripted check that every new async state change writes into a live region or moves focus (WCAG 3.3.1–3.3.4, 4.1.3)[^wcag22]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Whether the error text suggests a correction                                                         |
| `org-fit-i18n`                             | Internationalization                           | Are new user-visible strings in the catalog as whole messages, is locale-dependent output produced from locale data, and does the surface tolerate RTL, script variation and text expansion? | major    | diff  | high      | adjudicate | **Inert unless the capability probe finds a message catalog or a declared multi-locale claim.** `i18next/no-literal-string`, `formatjs/no-literal-string-in-jsx`, plus re-run extraction and `git diff --exit-code` on the catalogs; `ast-grep`/Semgrep for ternary pluralization, `.toFixed()`, `'$' + amount`, `toLocale*` with no locale argument, `toLowerCase()` comparison, `sort()` on display strings, `', '` list joins; W3C i18n Checker against changed routes; a stylelint ban on physical properties in new component CSS; `editorconfig-checker` `charset`; a pseudo-localization build and screenshot diff as the runtime backstop[^icu-messages][^cldr-plurals][^ecma402][^w3c-i18n-checker][^editorconfig-checker][^semgrep]                                         | Whether a flagged string is genuinely user-visible                                                   |
| `org-fit-performance-budget-regression`    | Declared budget breached or unguarded          | Does the change push a **declared** budget past its threshold — or add a shipped artifact or user-facing surface no budget enumerates?                                                       | major    | diff  | very-high | skip       | **Scope: declared budgets on shipped artifacts and user-facing surfaces.** Lighthouse CI `assert`, `size-limit`/`bundlesize`, webpack `performance` hints (250 KB default)[^lighthouse-ci][^webdev-budgets][^web-vitals]; plus the set-difference absorbed from the deleted `org-fit-budget-coverage-gap` — routes/entrypoints/artifacts produced by the build minus those enumerated in `lighthouserc.*`/`size-limit`/release-pipeline config. Server-side cost is **not** here: per-request algorithmic cost is `correctness-algorithmic-cost`, shared-dependency load is `process-load-capacity-validation`                                                                                                                                                                        | none                                                                                                 |
| `org-fit-platform-assumption-leakage`      | Single-platform assumptions in code            | Does the code hardcode one OS, arch, shell or filesystem's behaviour?                                                                                                                        | major    | diff  | very-high | adjudicate | `shellcheck -s sh`; Semgrep for GNU-only flags and `/`-concatenated paths; `docker buildx imagetools inspect` asserting required platforms; case-collision check (`git ls-files \| tr A-Z a-z \| sort \| uniq -d`); `.gitattributes` `* text=auto` assertion[^shellcheck][^semgrep][^docker-multiplatform][^gitattributes]                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Whether the platform is actually in the support set                                                  |
| `org-fit-portability-matrix-gap`           | Claimed support unexercised by CI              | Is every platform and runtime version the project _claims_ to support actually exercised in CI?                                                                                              | major    | repo  | very-high | skip       | Parse declared support (`engines`, `os`, `python_requires`, `rust-version`, README table) into a set; parse `strategy.matrix` (+`include`/`exclude`) into a set; report the difference[^gh-actions-matrix]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | none                                                                                                 |
| `org-fit-environment-parity`               | Dev/CI/prod divergence                         | Does the change make development, CI and production diverge?                                                                                                                                 | major    | repo  | very-high | adjudicate | Extract service image/version strings from compose, devcontainer, CI service containers and IaC into a table and assert equality; grep env-name branching outside config modules; assert lockfile-respecting install commands in CI[^12factor-parity][^12factor-config]                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Whether a divergence is deliberate and bounded                                                       |

Grounded in: [^innersource-common-req][^innersource-base-docs][^cncf-platforms][^cncf-maturity][^google-look][^google-std][^swe-ch16][^swe-ch20][^12factor-config][^12factor-parity][^wcag22][^act-rules][^using-aria][^aria-apg][^guidepup][^icu-messages][^cldr-plurals][^ecma402][^w3c-i18n-checker][^web-vitals][^webdev-budgets][^editorconfig-spec][^gitattributes][^docker-multiplatform][^gh-actions-avoid-dup][^gh-actions-matrix][^renovate-presets][^go-mod-tidy-diff][^k8s-make-update][^api-linter][^aip180][^eslint-max-params][^sonar-metrics][^typescript-eslint][^editorconfig-checker][^axe-core][^jsx-a11y][^shellcheck][^lighthouse-ci][^conftest-opa][^syncpack][^semgrep]

---

## References

[^12factor-config]: _The Twelve-Factor App_, III. Config — the open-source litmus test. <https://12factor.net/config>

[^12factor-parity]: _The Twelve-Factor App_, X. Dev/prod parity. <https://12factor.net/dev-prod-parity>

[^act-rules]: W3C WAI, _ACT Rules_ (Accessibility Conformance Testing). <https://www.w3.org/WAI/standards-guidelines/act/rules/>

[^aip180]: Google AIP-180, _Backwards compatibility_ — renames, type changes, new required fields, default changes, tightened validation, semantic changes; see also the AIP index <https://google.aip.dev/general>. <https://google.aip.dev/180>

[^api-linter]: Google API Linter — AIP compliance checks, `--set-exit-status`, machine-readable output; see also `buf lint` and Spectral for OpenAPI. <https://linter.aip.dev/>

[^aria-apg]: W3C WAI, _ARIA Authoring Practices Guide: Read Me First_ — "No ARIA is better than Bad ARIA"; "a role is a promise". <https://www.w3.org/WAI/ARIA/apg/practices/read-me-first/>

[^axe-core]: Deque axe-core — accessibility engine for automated testing (~57% of WCAG issues, zero false positives by design — a floor, not a conformance claim); `pa11y` is an equivalent CLI runner. <https://github.com/dequelabs/axe-core>

[^cldr-plurals]: Unicode CLDR, _Plural Rules_ (zero/one/two/few/many/other). <https://cldr.unicode.org/index/cldr-spec/plural-rules>

[^cncf-maturity]: CNCF TAG App Delivery, _Platform Engineering Maturity Model_. <https://tag-app-delivery.cncf.io/whitepapers/platform-eng-maturity-model/>

[^cncf-platforms]: CNCF TAG App Delivery, _Platforms White Paper_ — golden paths; platforms optional and composable. <https://tag-app-delivery.cncf.io/whitepapers/platforms/>

[^conftest-opa]: Open Policy Agent / Conftest — policy over structured configuration; Checkov and KICS provide equivalent IaC and IAM policy scanning. <https://www.conftest.dev/>

[^docker-multiplatform]: Docker Docs, _Multi-platform builds_ — manifest lists, `docker buildx build --platform`. <https://docs.docker.com/build/building/multi-platform/>

[^ecma402]: ECMA-402, _ECMAScript Internationalization API Specification_ (`Intl.NumberFormat`, `DateTimeFormat`, `PluralRules`, `Collator`, `ListFormat`, `Segmenter`). <https://tc39.es/ecma402/>

[^editorconfig-checker]: editorconfig-checker — language-agnostic EditorConfig validation with CI exit codes and GitHub annotations. <https://github.com/editorconfig-checker/editorconfig-checker>

[^editorconfig-spec]: EditorConfig Specification (`indent_style`, `charset`, `end_of_line`, `insert_final_newline`). <https://spec.editorconfig.org/>

[^eslint-max-params]: ESLint, _max-params_ — default maximum of 3. <https://eslint.org/docs/latest/rules/max-params>

[^gh-actions-avoid-dup]: GitHub Docs, _Reusing workflow configurations_ — reusable workflows and composite actions. <https://docs.github.com/en/actions/concepts/workflows-and-actions/reusing-workflow-configurations>

[^gh-actions-matrix]: GitHub Docs, _Running variations of jobs in a workflow_ — `strategy.matrix`. <https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/run-job-variations>

[^gitattributes]: Git, _gitattributes_ — `text`, `text=auto`, `eol` for cross-platform line-ending normalisation. <https://git-scm.com/docs/gitattributes>

[^go-mod-tidy-diff]: Go 1.23 Release Notes — `go mod tidy -diff` (non-zero exit when updates are needed). <https://go.dev/doc/go1.23>

[^google-look]: Google, _What to Look For in a Code Review_ (eng-practices) — over-engineering as a type of complexity; tests in the same CL; docs updated with behaviour. <https://google.github.io/eng-practices/review/reviewer/looking-for.html>

[^google-std]: Google, _The Standard of Code Review_ (eng-practices) — design is not preference; the style guide is the absolute authority on style. <https://google.github.io/eng-practices/review/reviewer/standard.html>

[^guidepup]: Guidepup — _Virtual Screen Reader_ (screen-reader simulator with `spokenPhraseLog()` assertions) <https://github.com/guidepup/virtual-screen-reader>; and _Guidepup_ (real VoiceOver/NVDA automation from JavaScript) <https://github.com/guidepup/guidepup>

[^icu-messages]: ICU User Guide, _Formatting Messages_ — MessageFormat, plural/select arguments, why concatenation breaks translation. <https://unicode-org.github.io/icu/userguide/format_parse/messages/>

[^innersource-base-docs]: InnerSource Commons, _Standard Base Documentation_ pattern. <https://patterns.innersourcecommons.org/p/base-documentation>

[^innersource-common-req]: InnerSource Commons, _Common Requirements_ pattern. <https://patterns.innersourcecommons.org/p/common-requirements>

[^jsx-a11y]: eslint-plugin-jsx-a11y — static a11y rules for JSX; explicitly must be paired with runtime testing. <https://github.com/jsx-eslint/eslint-plugin-jsx-a11y>

[^k8s-make-update]: Kubernetes Contributors, _CI bot helper job: automating "make update"_ — regenerate-and-compare verification. <https://www.kubernetes.dev/blog/2022/03/15/k8s-triage-bot-helper-ci-job/>

[^lighthouse-ci]: Lighthouse CI, _Getting Started_ — `assert` failing the build; `collect.url` determines what is measured; `size-limit`/`bundlesize` are the equivalent asset gates. <https://github.com/GoogleChrome/lighthouse-ci/blob/main/docs/getting-started.md>

[^renovate-presets]: Renovate Docs, _Shared Config Presets_ — publish an org preset and `extends` it per repo. <https://docs.renovatebot.com/config-presets/>

[^semgrep]: Semgrep — _Writing rules_ (`patterns`, `pattern-not`, `pattern-inside`, metavariables) and _Taint mode_ (sources, sinks, sanitizers, propagators). <https://docs.semgrep.dev/writing-rules/overview> · <https://docs.semgrep.dev/writing-rules/data-flow/taint-mode/overview>

[^shellcheck]: ShellCheck — syntax and semantic shell analysis; `-s sh` for POSIX portability / bashism detection. <https://www.shellcheck.net/>

[^sonar-metrics]: SonarQube Server, _Metric definitions_ — Cognitive Complexity, Cyclomatic Complexity, class complexity, duplication thresholds, `duplicated_lines_density`. <https://docs.sonarsource.com/sonarqube-server/user-guide/code-metrics/metrics-definition.md>

[^swe-ch16]: _Software Engineering at Google_, Ch. 16 "Version Control and Branch Management" — Source of Truth, the One-Version Rule. <https://abseil.io/resources/swe-book/html/ch16.html>

[^swe-ch20]: _Software Engineering at Google_, Ch. 20 "Static Analysis" — mechanical issues belong in tools, not reviewers. <https://abseil.io/resources/swe-book/html/ch20.html>

[^syncpack]: syncpack — detect and fix dependency-version mismatches across a monorepo (`list-mismatches`). <https://jamiemason.github.io/syncpack/>

[^typescript-eslint]: typescript-eslint rules — `no-unsafe-assignment` (and `no-unsafe-argument`/`-call`/`-member-access`/`-return`, `no-explicit-any`), `no-non-null-assertion`, `no-floating-promises`, `switch-exhaustiveness-check`, `no-unnecessary-condition`. <https://typescript-eslint.io/rules/>

[^using-aria]: W3C, _Using ARIA_ — the Rules of ARIA Use. <https://www.w3.org/TR/using-aria/>

[^w3c-i18n-checker]: W3C, _About the Internationalization (i18n) Checker_. <https://validator.w3.org/i18n-checker/about>

[^wcag22]: W3C, _Web Content Accessibility Guidelines (WCAG) 2.2_ (SC 1.1.1, 1.2.x, 1.3.1, 1.4.3, 1.4.4, 1.4.10, 2.1.1, 2.3.3, 2.4.4, 2.4.6, 2.5.8, 3.3.1–3.3.4, 4.1.3). <https://www.w3.org/TR/WCAG22/>

[^web-vitals]: web.dev, _Web Vitals_ — LCP 2.5s, INP 200ms, CLS 0.1 at the 75th percentile. <https://web.dev/articles/vitals>

[^webdev-budgets]: web.dev, _Incorporate performance budgets into your build process_ — Lighthouse budgets, webpack `performance` hints (250 KB default), `bundlesize`. <https://web.dev/articles/incorporate-performance-budgets-into-your-build-tools>
