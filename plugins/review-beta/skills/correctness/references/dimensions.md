# Dimensions — `correctness` family

Extracted verbatim from the canonical review taxonomy
(`.ai-agent-jack/docs/research/review-taxonomy/taxonomy.md`, "Family: correctness").
Column meanings, the severity rubric and the de-duplication ledger live in that source;
this file carries only this family's table and the citations it depends on.

## Contents

1. [Dimension table](#family-correctness)
2. [References](#references)

## Family: correctness

Logic defects, edge cases, error handling, concurrency, resource lifecycle, data loss, idempotency,
domain invariants, algorithmic cost, failure modes, and the tests that would have caught each.
**19 dimensions.**

| id                                      | name                                         | the question                                                                                                                                                              | severity | scope | tier      | agent      | automatable-with                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | residual                                                                                                                   |
| :-------------------------------------- | :------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :------- | :---- | :-------- | :--------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------- |
| `correctness-boundary-conditions`       | Boundary & equivalence-class handling        | Does this behave correctly at the edges of every input partition, not just the middle?                                                                                    | blocker  | diff  | partial   | adjudicate | Property/fuzz harness required for each new pure function above a complexity threshold — `go test -fuzz` with a seed corpus, Hypothesis `@given`, `fast-check fc.assert`; corpus in PR CI, fuzzer nightly; crashers map to this id[^go-fuzz][^hypothesis]; mutation survivors at boundary expressions                                                                                                                                                                                                                                                                                   | A changed function has no harness and cannot easily get one                                                                |
| `correctness-null-absence`              | Null / absent-value handling                 | Can any value dereferenced, indexed or unwrapped here be null, undefined, empty or absent?                                                                                | blocker  | diff  | high      | adjudicate | `strictNullChecks`, mypy `--strict`, `go vet nilness`, `no-non-null-assertion`, CWE-476 SAST[^tsconfig][^mypy][^govet][^typescript-eslint][^cwe476]                                                                                                                                                                                                                                                                                                                                                                                                                                     | Untyped boundaries the checker cannot see — JSON parse, FFI, `any` at an API edge                                          |
| `correctness-numeric-precision`         | Numeric range & precision                    | Can any arithmetic here overflow, wrap, truncate, divide by zero or lose decimal precision?                                                                               | blocker  | diff  | high      | adjudicate | Re-run the existing suite under trapping arithmetic: C/C++ `-fsanitize=undefined,integer,implicit-conversion`; Rust `RUSTFLAGS="-C overflow-checks=yes"` + clippy `cast_possible_truncation`/`cast_sign_loss`/`arithmetic_side_effects`[^rust-lints]; Java `-Xlint:all` + Semgrep requiring `Math.addExact`/`multiplyExact` on money-typed fields; Python `decimal` with `getcontext().traps[Inexact]=True` and `-W error`; CWE-190 SAST[^cwe190][^goldberg]                                                                                                                            | A path the trapping run does not cover — which `correctness-test-coverage` already decides                                 |
| `correctness-error-propagation`         | Error detection & propagation                | Is every failure signal checked, and does an unhandled failure actually stop the operation?                                                                               | blocker  | diff  | high      | adjudicate | `errcheck`, `unused_must_use`, `no-floating-promises`, empty-catch Semgrep (CWE-252/390/754)[^errcheck][^rust-lints][^typescript-eslint][^semgrep][^cwe252][^cwe390][^cwe754]                                                                                                                                                                                                                                                                                                                                                                                                           | Whether a caught-and-logged error should have aborted the operation                                                        |
| `correctness-resource-lifecycle`        | Resource lifecycle on every exit path        | Is every acquired resource released on the error path, not just the happy path?                                                                                           | major    | diff  | partial   | adjudicate | `go vet lostcancel`/`copylocks`, acquire-without-scope-guard Semgrep, leak-detector assertions (CWE-772)[^govet][^semgrep][^cwe772][^go-context]                                                                                                                                                                                                                                                                                                                                                                                                                                        | Ownership across an async boundary                                                                                         |
| `correctness-concurrency`               | In-process concurrency & shared state        | Can two flows reach this state simultaneously, and is every check-then-act atomic?                                                                                        | blocker  | diff  | high      | adjudicate | `go test -race`, `-fsanitize=thread`, `go vet copylocks`/`loopclosure`, TOCTOU SAST (CWE-362/367/833)[^go-race][^tsan][^govet][^cwe362][^cwe367][^cwe833]                                                                                                                                                                                                                                                                                                                                                                                                                               | Interleavings the existing tests never schedule                                                                            |
| `correctness-control-flow-completeness` | Control-flow completeness & reachability     | Is every case of every union/enum handled, and is every branch reachable?                                                                                                 | major    | diff  | very-high | adjudicate | `switch-exhaustiveness-check`, compiler-enforced Rust `match`, `go vet unreachable`, CWE-561/570/571[^typescript-eslint][^govet][^cwe561]                                                                                                                                                                                                                                                                                                                                                                                                                                               | Whether the default arm is a correct behaviour or a silent swallow                                                         |
| `correctness-domain-invariants`         | Domain invariants & legal state              | Does this change make an illegal state reachable, or break a conservation/ordering invariant the domain guarantees?                                                       | blocker  | diff  | partial   | judge      | Property-based state-machine tests on the changed aggregate (Hypothesis `RuleBasedStateMachine`, `rapid`, fast-check model-based)[^hypothesis]; DB `CHECK`/partial-unique constraints for new numeric and status columns; Semgrep for direct `entity.status = X` assignment outside the declared transition function; transition-matrix lint requiring every new enum member to appear in the matrix[^semgrep]                                                                                                                                                                          | Naming the invariant and the transition that violates it — the emitted recommendation is the property test that encodes it |
| `correctness-retry-timeout`             | Timeout, retry & backoff discipline          | Does every remote call have a deadline, and is every retry bounded, jittered and non-amplifying?                                                                          | major    | diff  | partial   | adjudicate | Semgrep: client constructed without timeout; retry loop with no jitter, no cap, or no propagated deadline[^semgrep][^aws-jitter][^azure-retry][^azure-cb][^sre-overload]                                                                                                                                                                                                                                                                                                                                                                                                                | Whether the aggregate retry budget across layers amplifies[^sre-cascading]                                                 |
| `correctness-idempotency`               | Idempotency & duplicate-delivery safety      | If this runs twice with the same input, is the outcome the same?                                                                                                          | blocker  | diff  | high      | adjudicate | A checked-in replay harness, not a filename check: snapshot the datastore, invoke the handler with a recorded request, snapshot, re-invoke with the identical request and idempotency key, assert `snapshot(2) == snapshot(3)` byte-for-byte and that emitted side-effect counts are unchanged (testcontainers + the existing DB dump tooling). `.review/replay/<handler>.yaml` fixtures join the `correctness-test-coverage` partition map, so a new mutating route with no fixture fails[^aws-idempotency][^aws-sqs]                                                                  | Side effects the snapshot does not capture (outbound mail, third-party charges)                                            |
| `correctness-partial-failure`           | Partial failure & degradation                | If one of N steps, shards or dependencies fails, what state is the system left in?                                                                                        | major    | diff  | partial   | judge      | Semgrep `Promise.all`/`errgroup` around side effects, mutate-then-`return err` loops; fault-injection test presence[^semgrep][^sre-cascading]                                                                                                                                                                                                                                                                                                                                                                                                                                           | Whether the resulting partial state is acceptable to the domain                                                            |
| `correctness-transactional-integrity`   | Transactional atomicity & concurrent writes  | Are multi-step mutations atomic, and is read-modify-write safe against a concurrent writer?                                                                               | blocker  | diff  | high      | adjudicate | PostgreSQL `isolationtester` / `pg_isolation_regress` spec files (sessions, steps, permutations)[^pg-isolation][^pg-iso], or Elle/Jepsen for distributed stores. CI predicate: a diff touching `BEGIN`/`SET TRANSACTION ISOLATION`/`SELECT … FOR UPDATE` or a repository-layer read-modify-write requires a matching spec under the isolation-test root. Semgrep: SELECT-then-UPDATE without `FOR UPDATE`/version predicate; isolation level set with no `40001` branch[^semgrep]                                                                                                       | Which permutation the spec omitted                                                                                         |
| `correctness-data-loss`                 | Destructive operations & data recoverability | If this is wrong, is the data recoverable — and how quickly?                                                                                                              | blocker  | diff  | high      | adjudicate | CI grep/Semgrep over migrations + IaC for `DROP`/`TRUNCATE`/`rm -rf`/retention cuts, gated on an explicit allow-destructive annotation[^semgrep][^sre-data]                                                                                                                                                                                                                                                                                                                                                                                                                             | Whether the stated recovery path has actually been exercised                                                               |
| `correctness-algorithmic-cost`          | Per-request algorithmic & IO cost            | Does this path do work that grows super-linearly with input, or per-item IO where per-batch IO exists?                                                                    | major    | diff  | partial   | adjudicate | Semgrep/ast-grep: `await`/query inside a loop over request-sized data, ORM lazy-load access in a loop, nested loop over two request collections[^semgrep]; query-count assertions in integration tests (`assertNumQueries`, `pg_stat_statements` delta); `EXPLAIN` on new/changed queries in migration CI failing on `Seq Scan` above N rows; index-presence check for every new `WHERE`/`ORDER BY` column                                                                                                                                                                              | Whether the input is actually request-sized in production                                                                  |
| `correctness-type-safety-escapes`       | Type-system escape hatches                   | Where does this switch the type checker off, and what runtime invariant is assumed in exchange?                                                                           | major    | diff  | very-high | adjudicate | `no-unsafe-*`, `no-explicit-any`, mypy `--disallow-any-explicit` + `--warn-unused-ignores`, diff-scoped suppression-comment grep[^typescript-eslint][^mypy]                                                                                                                                                                                                                                                                                                                                                                                                                             | Justification of each suppression                                                                                          |
| `correctness-time-clock`                | Time, clock & timezone correctness           | Does this measure duration monotonically and store instants unambiguously?                                                                                                | major    | diff  | high      | adjudicate | Semgrep: wall-clock subtraction, naive `datetime.now()`, `+ 86400` arithmetic, local-time on a persistence path; `go vet timeformat`[^semgrep][^govet][^go-time]                                                                                                                                                                                                                                                                                                                                                                                                                        | DST and leap-second semantics of the business rule                                                                         |
| `correctness-termination-bounds`        | Termination & bounded growth                 | Is every loop, recursion, queue, buffer and accumulator bounded by something other than hope?                                                                             | major    | diff  | high      | adjudicate | Counterexamples, not proofs: per-test wall-clock caps and a hard memory ceiling on every CI run — `go test -timeout=30s` with `GOMEMLIMIT` + `debug.SetMaxStack`, `pytest-timeout --timeout=30 --timeout-method=thread` under `ulimit -v`, Node `--max-old-space-size=N`, JVM `-Xmx<N> -XX:+ExitOnOutOfMemoryError`. Surface OOM-kill (exit 137) as a distinct failure class mapped to this id. Semgrep `while True` without break, append-in-loop over request-sized data, unbounded channel; CWE-674 SAST[^semgrep][^cwe674]                                                          | The loop is bounded, but by a request-sized input — routes to `sec-resource-exhaustion-limits` if reachable by an attacker |
| `correctness-test-coverage`             | Test accompanies the change, and covers it   | Does this diff add or change behaviour without a test that executes it — including the failure branches?                                                                  | major    | diff  | very-high | skip       | Danger rule over `git diff --name-only base...head` partitioned by test-root globs; `t.Skip`/`it.skip`/`@Ignore`/`xfail` grep on added lines; `diff-cover --fail-under=N` or Codecov patch status with branch coverage enabled[^danger][^diffcover][^codecov]                                                                                                                                                                                                                                                                                                                           | none                                                                                                                       |
| `correctness-test-quality`              | Assertion strength, determinism & tier       | If the new logic were subtly wrong would any test turn red, will the verdict be the same on every run, and is it verified at the cheapest tier that can catch the defect? | major    | diff  | high      | adjudicate | Mutation testing scoped to the diff (PIT, Stryker) on covered non-arid lines[^pitest][^stryker][^google-mutation]; assertion-free-test Semgrep; `go test -count=N -shuffle=on`, `pytest --count`/`-p randomly`[^google-flaky-2020]. Tier enforced at **runtime**, not by import grep: `pytest-socket --disable-socket --allow-unix-socket` on the unit tier[^pytest-socket], `nock.disableNetConnect()` / msw `onUnhandledRequest: 'error'`, Go `-short` + a panicking `http.DefaultTransport` in `TestMain`, Bazel `size` sandboxing where available[^bazel-attrs][^google-test-sizes] | Whether a surviving mutant is a real gap or an equivalent mutant                                                           |

Grounded in: [^cwe476][^cwe252][^cwe190][^cwe754][^cwe390][^cwe772][^cwe362][^cwe367][^cwe833][^cwe561][^cwe674][^istqb-boundary][^goldberg][^pg-iso][^pg-isolation][^go-time][^go-context][^go-fuzz][^hypothesis][^aws-idempotency][^aws-sqs][^aws-jitter][^azure-retry][^azure-cb][^sre-cascading][^sre-overload][^sre-data][^fowler-parallel][^nist235][^google-mutation][^google-flaky-2017][^google-flaky-2020][^google-test-sizes][^bazel-attrs][^pytest-socket][^rust-lints][^errcheck][^typescript-eslint][^tsconfig][^mypy][^govet][^go-race][^tsan][^semgrep][^codecov][^diffcover][^pitest][^stryker][^danger][^pyramid]

---

## References

[^aws-idempotency]: AWS Builders' Library, _Making retries safe with idempotent APIs_. <https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/>

[^aws-jitter]: AWS Architecture Blog, _Exponential Backoff And Jitter_. <https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/>

[^aws-sqs]: AWS, _Amazon SQS standard queues_ — at-least-once delivery, duplicate messages. <https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/standard-queues.html>

[^azure-cb]: Microsoft, _Circuit Breaker pattern_ — Azure Architecture Center. <https://learn.microsoft.com/en-us/azure/architecture/patterns/circuit-breaker>

[^azure-retry]: Microsoft, _Retry pattern_ — Azure Architecture Center. <https://learn.microsoft.com/en-us/azure/architecture/patterns/retry>

[^bazel-attrs]: Bazel, _Common definitions — test attributes_ (`size` default `medium`, timeouts, `flaky`, `shard_count`). <https://bazel.build/reference/be/common-definitions>

[^codecov]: Codecov, _Commit status — patch vs project_. <https://docs.codecov.com/docs/commit-status>

[^cwe190]: MITRE, _CWE-190: Integer Overflow or Wraparound_. <https://cwe.mitre.org/data/definitions/190.html>

[^cwe252]: MITRE, _CWE-252: Unchecked Return Value_. <https://cwe.mitre.org/data/definitions/252.html>

[^cwe362]: MITRE, _CWE-362: Concurrent Execution using Shared Resource with Improper Synchronization ("Race Condition")_. <https://cwe.mitre.org/data/definitions/362.html>

[^cwe367]: MITRE, _CWE-367: Time-of-check Time-of-use (TOCTOU) Race Condition_. <https://cwe.mitre.org/data/definitions/367.html>

[^cwe390]: MITRE, _CWE-390: Detection of Error Condition Without Action_. <https://cwe.mitre.org/data/definitions/390.html>

[^cwe476]: MITRE, _CWE-476: NULL Pointer Dereference_. <https://cwe.mitre.org/data/definitions/476.html>

[^cwe561]: MITRE, _CWE-561: Dead Code_ (with CWE-570 / CWE-571). <https://cwe.mitre.org/data/definitions/561.html>

[^cwe674]: MITRE, _CWE-674: Uncontrolled Recursion_. <https://cwe.mitre.org/data/definitions/674.html>

[^cwe754]: MITRE, _CWE-754: Improper Check for Unusual or Exceptional Conditions_. <https://cwe.mitre.org/data/definitions/754.html>

[^cwe772]: MITRE, _CWE-772: Missing Release of Resource after Effective Lifetime_. <https://cwe.mitre.org/data/definitions/772.html>

[^cwe833]: MITRE, _CWE-833: Deadlock_. <https://cwe.mitre.org/data/definitions/833.html>

[^danger]: Danger JS — Dangerfile rules: changed-file pairing, changelog requirement, oversized-PR warning, test-file verification. <https://danger.systems/js/>

[^diffcover]: _diff_cover_ — find diff lines needing coverage; `--fail-under`. <https://github.com/Bachmann1234/diff_cover>

[^errcheck]: kisielk, _errcheck_ — finds silently ignored errors in Go. <https://github.com/kisielk/errcheck>

[^fowler-parallel]: Fowler, _ParallelChange_ — expand → migrate → contract; releasable in any phase. <https://martinfowler.com/bliki/ParallelChange.html>

[^go-context]: The Go Authors, package `context` — CancelFunc leaks, deadline propagation. <https://pkg.go.dev/context>

[^go-fuzz]: The Go Authors, _Go Fuzzing_ — edge cases, corpus minimisation, deadlock/infinite-loop detection. <https://go.dev/doc/security/fuzz/>

[^go-race]: The Go Authors, _Data Race Detector_. <https://go.dev/doc/articles/race_detector>

[^go-time]: The Go Authors, package `time` — monotonic clocks. <https://pkg.go.dev/time>

[^goldberg]: Goldberg, _What Every Computer Scientist Should Know About Floating-Point Arithmetic_. <https://docs.oracle.com/cd/E19957-01/806-3568/ncg_goldberg.html>

[^google-flaky-2017]: Listfield, Google Testing Blog, _Where do our flaky tests come from?_ — ~4.2M tests; flakiness rises with test binary size; when a stable test went flaky **and** the flake was traceable to a specific code change, the cause was a production-code bug 1/6th of the time. <https://testing.googleblog.com/2017/04/where-do-our-flaky-tests-come-from.html>

[^google-flaky-2020]: Google Testing Blog, _Test Flakiness — One of the main challenges of automated testing_. <https://testing.googleblog.com/2020/12/test-flakiness-one-of-main-challenges.html>

[^google-mutation]: Petrović & Ivanković, _State of Mutation Testing at Google_, ICSE-SEIP 2018 — diff-based mutation analysis; "arid" lines. <https://research.google/pubs/state-of-mutation-testing-at-google/>

[^google-test-sizes]: Google Testing Blog, _Test Sizes_ — small 60 s, no network/DB/FS/threads; medium 300 s; large 900 s **or more**. <https://testing.googleblog.com/2010/12/test-sizes.html>

[^govet]: The Go Authors, _`go vet`_ — `nilness`, `lostcancel`, `copylocks`, `loopclosure`, `unreachable`, `unusedresult`, `errorsas`, `timeformat`. <https://pkg.go.dev/cmd/vet>

[^hypothesis]: Hypothesis — property-based testing for Python, including `RuleBasedStateMachine` for stateful/model-based testing. <https://hypothesis.readthedocs.io/en/latest/>

[^istqb-boundary]: ISTQB, _Glossary — boundary value_. <https://istqb-glossary.page/boundary-value/>

[^mypy]: mypy, _Command line_ — `--strict`, `--disallow-any-explicit`, `--disallow-untyped-defs`, `--warn-unused-ignores`. <https://mypy.readthedocs.io/en/stable/command_line.html>

[^nist235]: Watson & McCabe, _Structured Testing: A Testing Methodology Using the Cyclomatic Complexity Metric_, NIST SP 500-235 (1996). <https://nvlpubs.nist.gov/nistpubs/Legacy/SP/nistspecialpublication500-235.pdf>

[^pg-iso]: PostgreSQL, _Transaction Isolation_ — lost updates, serialization failures, mandatory `40001` retry. <https://www.postgresql.org/docs/current/transaction-iso.html>

[^pg-isolation]: PostgreSQL, _`src/test/isolation` README_ — `isolationtester` and `pg_isolation_regress`; spec files declaring `setup`, per-session `step`s, `permutation` lines, and `teardown`. <https://github.com/postgres/postgres/tree/master/src/test/isolation>

[^pitest]: PIT — real-world mutation testing and mutation score. <https://pitest.org/>

[^pyramid]: Fowler / Vocke, _The Practical Test Pyramid_. <https://martinfowler.com/articles/practical-test-pyramid.html>

[^pytest-socket]: pytest-socket — disables all network calls through Python's `socket` interface during tests; `--disable-socket`, `--allow-unix-socket`, per-test allowlists. <https://github.com/miketheman/pytest-socket>

[^rust-lints]: The Rust Compiler, _Lint listing — warn-by-default_ (`unused_must_use`), and `std::result::Result`. <https://doc.rust-lang.org/rustc/lints/listing/warn-by-default.html> · <https://doc.rust-lang.org/std/result/enum.Result.html>

[^semgrep]: Semgrep — _Writing rules_ (`patterns`, `pattern-not`, `pattern-inside`, metavariables) and _Taint mode_ (sources, sinks, sanitizers, propagators). <https://docs.semgrep.dev/writing-rules/overview> · <https://docs.semgrep.dev/writing-rules/data-flow/taint-mode/overview>

[^sre-cascading]: Beyer et al. (eds.), _Site Reliability Engineering_, Ch. 22 "Addressing Cascading Failures" — retry amplification, graceful degradation. <https://sre.google/sre-book/addressing-cascading-failures/>

[^sre-data]: _Site Reliability Engineering_, Ch. 26 "Data Integrity" — soft deletion, tiered backups, replication is not recoverability. <https://sre.google/sre-book/data-integrity/>

[^sre-overload]: _Site Reliability Engineering_, Ch. 21 "Handling Overload" — per-request retry limits, 10% client retry budget. <https://sre.google/sre-book/handling-overload/>

[^stryker]: Stryker Mutator — mutation testing for JS/TS, C#, Scala. <https://stryker-mutator.io/docs/>

[^tsan]: LLVM/Clang, _ThreadSanitizer_. <https://clang.llvm.org/docs/ThreadSanitizer.html>

[^tsconfig]: TypeScript, _TSConfig Reference_ — `strictNullChecks`, `noUnusedLocals`, `noUnusedParameters`. <https://www.typescriptlang.org/tsconfig/>

[^typescript-eslint]: typescript-eslint rules — `no-unsafe-assignment` (and `no-unsafe-argument`/`-call`/`-member-access`/`-return`, `no-explicit-any`), `no-non-null-assertion`, `no-floating-promises`, `switch-exhaustiveness-check`, `no-unnecessary-condition`. <https://typescript-eslint.io/rules/>
