# Plugin: web-auto-approve

**Purpose**: Auto-approve `Edit`, `Write`, and `Bash` permission requests in Claude Code web sessions.

## Hooks

- `PermissionRequest` (`bash`) — Auto-approve Edit/Write/Bash permission requests in web sessions
