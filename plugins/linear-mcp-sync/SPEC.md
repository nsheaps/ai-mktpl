# Plugin: linear-mcp-sync

**Purpose**: A Claude Code plugin that installs the Linear MCP server and adds hooks to prevent stale ticket updates through hash validation.

## Hooks
- `linear-hash-check` (`bash`) — linear-hash-check
- `linear-hash-save` (`bash`) — linear-hash-save
