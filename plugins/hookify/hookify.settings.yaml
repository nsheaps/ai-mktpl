# hookify default settings
# Override these in $CLAUDE_PROJECT_DIR/.claude/plugins.settings.yaml
enabled: true

# Files to watch for changes (basename patterns)
# watchFiles:
#   - "package.json"
#   - "requirements.txt"
#   - "Cargo.toml"
#   - "go.mod"

# Message to show when a watched file changes
# reminderMessage: "Dependencies changed. Check if license disclosures need updating."

# Files the agent should review when a watched file changes
# checkFiles:
#   - "path/to/LicensesComponent.tsx"
