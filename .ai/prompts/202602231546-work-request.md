- What happened to delegate mode with claude code?
- check claude changelog and ai-mktpl and see if anything needs updating
- updates were made in this repo to run-claude-team-persistent, update the ephemeral one appropriately and commit it
  - also make sure args are passed into it so I can pass `--resume 1234` or a specific prompt.
- Lets move op-exec from ai-mktpl to it's own repo like claude-utils, git-ws, agent-team, etc.
    - make it publish to nsheaps/homebrew-devsetup like the others
    - install it on this machine
- like homebrew-devsetup, make sure the following repos use mise tasks (or whatever pre-existing) to run a "check" github workflow that will autofix and push fixes if any are made. For any repo that doesn't use mise, or the scripts aren't organized as such, create an issue on that repo to resolve it. extract anything shared-shareable to nsheaps/github-actions and use across the organization.
    - nsheaps/ai-mktpl
    - nsheaps/agent-team
    - nsheaps/claude-utils
    - nsheaps/op-exec (new, public)
    - nsheaps/.github
    - nsheaps/.org (new, private)
    - nsheaps/git-ws
    - nsheaps/homebrew-devsetup
    - nsheaps/iac
    - nsheaps/.claude (new, private)
    If you can, use workflows in nsheaps/.github to sync these across repos.
- create nsheaps/.org to hold org-level resources, starting with linter configs
- Review open PRs across all repos in nsheaps org. If they're in draft and ready to go, move them to open and leave a review.
    - Because I cannot review PRs opened by me, if the PR is reviewed and looks good to merge, and is in the open state and CI is passing, and there's no outstanding feedback or things that need to be addressed in the pr, add the "ready" label for me to review.
- set up the arcane action (in PR https://github.com/nsheaps/github-actions/pull/1) in nsheaps/iac to deploy to arcane. It will be replacing portainer.
- create a nsheaps/.claude repo similar to dotfiles that is a repo containing symlinks to files in the user's home folder that are relevant, such as some of the symlinks that exist in ~/.claude already, agents, config, skills, rules, tasks, teams, todos, history.jsonl, ~/.claude.json, settings.json, settings.local.json, CLAUDE.md, CLAUDE.local.md, etc. Also set this repo up with mise scripts, and create a script called "swap-source-of-truth" that uses gum interactively to prompt you, showing you current state and then swapping, symlinks from the repo to the home folder, or from the home folder to the repo. Allow for advanced selection of folders so not everything has to be swapped.
  - in ai-mktpl create a plugin that will look for a config in ~/.claude/settings.remote-config.yaml
    In that config should say the upstream repo.
    An env var can set the upstream repo as well
    And the plugin should (on session start) look to sync that to ~/.claude-remote/, and ensure it is up to date with the latest on the default branch. If it cannot be updated cleanly, print error, suggesting that claude should fix, but ask the user if claude should fix it.
    If it updates cleanly, it should print, stating that it was updated. (if tagged, print tag and short sha, else just short sha. If config is set to verbose:true, then also print the commit titles since the last update, with the last one printed being most recent)
    swap-source-of-truth (as well as a link command) should default to using symlinks inside the relevant folders, rather than wholesale replacing them. always call the folder `.org`, regardless of any existing pattern you see right now. If .org already exists print a warning, and offer to link to .org1 or a custom name, or to replace.
    - install the plugin in the user config, setting nsheaps/.claude as the upstream.
- in nsheaps/.github, create a workflow that syncs labels across all repos where possible, preferably also setting them in at the org level (which unfortunately only applies to new repos). Make that workflow run a re-usable action for this from nsheaps/github-actions
- audit all issues across all nsheaps/ repo. Ensure they all have a "needs-human-attention" label if they need my input. In this repo/.claude/scratch/... keep track of and keep updated with the list for me to review
- in nsheaps/.github, create a workflow that syncs one-pass items to repo secrets, using reusable actions (and workflows?) from nsheaps/github-actions. It should treat the item as the entire list of secrets to sync (not deleting any), converting field names appropriately.
  - it should also use op-exec from before to recursively resolve all secrets, so the secret used to sync the repo itself can just be references to other secrets.
- create a plugin in ai-mktpl for a skill-based and hook-based workflow that can be used for me to word-vomit thoughts and ideas somewhere and an agent launched in a specific way can be my exec-assist to review those things and work with teammates to get it properly categorized. Any auto-categorization should then update the original text I entered with strikethrough and a sub-bullet linking to the new ticket.
  - Also create an agent prompt for the exec-assist in agent-team
- in agent-team, create the following folder structure and update the repo accordingly to move our looney toons over there.
  - templates/
    - agents/
    - teams/
    - system-prompts/<slug>/PROMPT.md (with @references to build with reusable pieces)
- in ai-mktpl, make a plugin (I think this is in ai ramblings or ideas in nsheaps/obsidian-vaults) called "skill-required".
  - use a hook to track when a specific skill is used, stored in json in ~/.claude/cache/plugins/skill-required/$projectSlug/...
    - Every time the tool is reading the skill, reset the counter for that skill, if it's a different skill dont incr, otherwise, increment the counter for all skills in the config
  - read config from user, with repo overriding, from .claude/settings.skill-required.yaml (or json) like we had before with settings.remote.yaml. Also read from settings.plugins.yaml with the plugin name as a header for it's settings. Make sure this pattern is well documented in docs in ai-mktpl.
    - default settings:
      ```yaml
        skill-required:
            enabled: true # Repo can turn it off, so can settings.local.json by setting CLAUDE_PLUGIN_SKILL_REQUIRED_ENABLED=false
            skills:
              - name: "
        ```
  - use a hook to check when a command is being run (same syntax as permissions in settings), that a skill was called within a configurable number of tool uses prior. If it hasn't, reject the tool use and say that the skill must be read before using that command.
- in ai-mktpl make a plugin that configures claude observability and otel in ~/.claude/settings.local.json
  - hook session start, use jq to modify in-place (create if needed) to set env vars
  - config like plugin above, for otel settings to set.
  - Plugin should have docs and skills for guiding the user through installing at least 2+ different local options to observe otel metrics/logs/traces (at least one with data persistence and one that is streaming only), and pointers to providers for remote setups.
- in agent-team, create a shell of docs for CLI commands (and for claude-team) for me to start word vomiting into (to make better). I at least want:
  - claude-team
    - init --from-template nsheaps/agent-team templates/teams/software-engineering/ --name <new name>
    - init --from-remote nsheaps/.org agent-teams/software-engineering/ --name <new name>
    - start ( also resumes, for that team, the last session ID for the orchestrator )
  - also create some mermaid diagrams for different orchestration mechanisms (like k8s vs local)

Make sure any new repos created today are also added to ~/src/nsheaps/workspaces/ai.code-workspace

Keep me honest, tell me where I mess up, realize I'm not watching you super closely, perform the work to the best of your ability.

You are here to be successful. To be successful, your work needs to be completed by you or a teammate. Work together to not duplicate work. Work together to get work done in parallel. Work together to use teammates as subject matter experts and owners for their domain. Use your best judgement with how far you go.

Make sure relevant research is captured in agent-team/docs/research, NOT .claude/tmp/ or .claude/scratch/.
Use worktrees (do not create teammates in isolation) and git-spice to create stacked branches where necessary, track all branches with git-spice, but keep as flat as possible (except where an expected conflict would arise to dependency)
Always commit work. Keep commits small, and commit often. Try to ensure CI will pass before pushing code, but feel free to separate into multiple commits.
NEVER force push to change history (EXCEPT by using gs stack submit)
Be cost conscious regarding how frequently you push, to limit CI costs, but the task is not complete until work is pushed (and eventually merged).
Make sure to track all work in each repo individually using issues. the PM can help coordinate but ultimately it's up to the team member to keep them up to date.
ALWAYS delegate the tasks to the teammates
NEVER do this work yourself.
