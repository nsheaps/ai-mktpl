Includes skills and hooks for symlinking conversation json and internal claude.md files into a folder that also contains scratch notes for the agent.

Potentially includes a background agent for maintaining these docs

Potentially includes hooks for PostToolUse(matching TodoWrite) to sync back or remind claude
